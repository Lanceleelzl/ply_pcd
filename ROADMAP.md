# Roadmap

## 当前阶段

```text
阶段 13：数据格式扩展
状态：已完成，格式接入、真实样本、Windows、Docker Linux 和独立 Chrome 验收通过
```

## 进行中

- 2026-09-16：阶段 13 已完成文件／ZIP／完整目录三种上传入口、服务端相对路径封装、格式探测、固定计算 LOD 的 XYZ 转换、Streamed SOG 浏览器流式显示和衍生目录生命周期。真实 Streamed SOG、LCC、LCC2 已完成 Windows 与 Docker Linux 解码、C++ 点云读取及容器服务验收；数据格式扩展的既定开发和验证项已完成。

- 2026-09-15：工作台已按确认设计完成第一轮布局调整，用户已确认并授权提交、推送。

- 2026-09-15：首页紧凑布局与矩阵双向编辑已实现并通过下述验证，用户已确认本轮效果并授权提交、推送。

- 外部 S3／MinIO 实例联调等待部署环境提供 endpoint、bucket 和凭据；代码与伪造客户端回归已经完成。
- 生产鉴权等待部署时生成并配置真实 API Key 摘要；鉴权代码、资源隔离和浏览器接入已经完成。
- 正式部署、版本 tag 与发布尚未安排，执行前按项目红线单独确认。

### 最近验证

- 2026-09-16（阶段 13 交付收口）：新增 `docs/STAGE13_ACCEPTANCE.md`，集中记录格式白名单、单文件／ZIP／目录 multipart 契约、固定计算 LOD 与 Gaussian 显示隔离、真实 Streamed SOG／LCC／LCC2 点数、Windows／Docker Linux 一致性、容器 ICP 和独立 Chrome 结果。明确目录上传的内部 ZIP、解压目录、计算 PLY 和显示转换资源会同时占用峰值磁盘，部署容量必须按总量规划。阶段顶部状态更新为已完成。

- 2026-09-16（阶段 13 Docker Linux 验收）：用户手动恢复 Docker Desktop 后确认 `desktop-linux` 为 Linux／amd64，当前分支镜像 `ply-pcd-registration:dev` 完整构建成功并由 compose 启动在 `127.0.0.1:8865`，首页与 OpenAPI 均返回 200。将三份真实数据以只读卷挂载到临时 Linux 容器，SplatTransform 3.4.2 按固定计算 LOD 分别解码 Streamed SOG 4,409,286 点、LCC 1,517,478 点、LCC2 448,957 点；镜像内 `registration_worker inspect-ply` 确认三者无效点均为 0，点数和包围盒与 Windows 结果一致。容器 API 完成 A／B 双角色业务坐标 ICP，RMS 分别为 `7.41627e-07` 和 `5.04159e-07`。独立 Playwright Chrome 打开容器首页，页面显示“服务正常”，文件／ZIP和数据集目录入口存在，控制台 0 error。compose 服务保持运行。

- 2026-09-16（阶段 13 真实数据验收）：使用用户提供的 987,836,547 字节 Streamed SOG、LCC 5.0 Portable 和 LCC2 0.0.3 实测。修正 Streamed SOG 探测：真实 `lod-meta.json` 无顶层 `version`，现按 `lodLevels`／`filenames`／`tree` 识别，并递归校验二叉树、块 SOG v2、WebP 引用以及每个块区间无缺口且不重叠。计算转换改为选择元数据声明的最粗固定层，避免先展开 1.0～1.2 亿点的 LOD 0；该选择独立于 Gaussian 显示和相机，并记录 `compute_lod`。SplatTransform 3.4.2 分别生成 4,409,286 点、1,517,478 点、448,957 点 PLY，Windows `registration_worker inspect-ply` 全部确认无无效点并返回有效包围盒。服务回归 94／94、可由 Node 直接执行的 Web 回归 109／109、类型检查、Web 构建和 Windows CTest 1／1 通过；`coordinate-query-views.test.ts` 因既有无扩展名导入不能由 Node ESM 直接加载，仍由类型检查和 Vite 构建覆盖。用户提供的 LCC 路径实际目录名为 `LCC_Results`，已按定位后的真实路径完成验收。

- 2026-09-16（阶段 13 文档与 OpenAPI 一致性）：README 已更新为普通／Gaussian／compressed PLY、PCD、LAS／LAZ、SOG、Streamed SOG、LCC、LCC2，以及单文件／ZIP／完整目录三种上传形态；API 文档明确 A／B 各自在单文件字段与重复目录字段之间二选一。直接读取当前 FastAPI OpenAPI，确认 `model_a`／`model_b` 为可空 binary，`model_a_files`／`model_b_files` 为可空 binary array，运行时负责条件必填校验。实施方案移除“仅 Gaussian PLY 可切换”的过期表述，路线图进行中事项收敛为真实 LCC／LCC2 样本和 Docker Linux 两项外部验收。

- 2026-09-16（阶段 13 LCC 边界与 Docker 复核）：依据 XGRIDS 格式说明和锁定版 SplatTransform 读取实现，LCC 仅接收 Portable／Quality，Quality 强制要求 `Shcoef.bin`；解压后为 Linux 大小写敏感环境补齐读取器要求的 `index.bin`／`data.bin`／`shcoef.bin` 规范硬链接，避免官方文档首字母大写名称在容器内解析失败。LCC2 增加 `totalLevels`、`totalSplats`、`lodSplats` 一致性和 `.ply`／`.spz`／`.sog` 块编码白名单校验。服务回归 94／94、差异检查通过。公开检索未找到可合法下载的真实 LCC／LCC2 场景包，真实样本验收继续保留。Windows Docker Desktop 4.60.0 已通过安装程序启动，但 backend 日志确认在初始化 Inference manager 时因 `dockerInference` 本地监听文件不可访问而崩溃，daemon API 不可用；未删除 Docker 文件、重置 Desktop 或修改系统配置，Linux 镜像验证继续阻塞。

- 2026-09-16（阶段 13 浏览器流式验收）：真实 Chrome 从首页选择包含 7 个文件的 Streamed SOG v1 目录作为模型 A、二进制 Gaussian PLY 作为模型 B，目录 multipart 上传返回 202，双方均解析为 4 点并进入工作台。首次点击 A 高斯暴露 Vite 未代理 `/gaussian-resources`，修复开发代理后 `lod-meta.json`、块 `meta.json` 和 5 个 WebP 资源全部返回 200，界面切换为“A：点云”并明确显示完整 Gaussian，控制台 0 error。保持 Gaussian 显示执行 ICP 成功，RMS 为 `0.000000 m`，证明显示切换未替代 XYZ 计算通道。截图：`output/playwright/stage13-streamed-sog.png`。类型检查、Web 构建及差异检查通过。

- 2026-09-16（阶段 13 第三批）：LCC／LCC2 准备流程拆分为独立的计算与显示输出：计算固定选择 LOD 0 生成标准 PLY，显示另行转换为保留 LOD 的 Streamed SOG 资源树。显示转换失败时保留 `xyz_status=ready` 并设置 `gaussian_status=failed`，不阻断中心点预览、粗配准或 ICP；令牌资源路由支持转换后的资源树。专项覆盖流式转换成功、显示转换失败和子资源访问，服务回归 93／93、类型检查及差异检查通过。当前仍缺真实 LCC／LCC2 数据验证，不能以模拟转换回归宣称格式验收完成。

- 2026-09-16（阶段 13 第二批）：首页为模型 A／B 增加独立的“选择文件／ZIP”和“选择数据集目录”入口，目录以重复 multipart 字段上传并保留浏览器提供的相对路径；服务端拒绝缺失、重复模式、越界路径和重复路径，将目录封装成内部 ZIP 后复用探测、对象归档与恢复链路。源释放新增清理 `datasets/`、`computed/`，恢复时若预览仍在但计算 PLY 缺失会重新排队生成。由 4 点二进制 Gaussian PLY 实际生成 Streamed SOG v1 目录，封装 ZIP 后成功识别入口、按 LOD 0 恢复标准 PLY并保留流式显示入口。服务 89／89、Web 108／108、Streamed SOG 集成 2／2、类型检查、Web 构建和差异检查通过；构建只有既有 PlayCanvas Worker 外部化与大包提示。

- 2026-09-15（阶段 13 第一批）：建立内容探测和数据集准备层，区分普通／Gaussian／PlayCanvas compressed PLY、bundled／unbundled SOG、Streamed SOG v1、LCC 5.0 和 LCC2 0.0.3；ZIP 校验入口、引用、路径、符号链接、文件数、解压大小及压缩比。固定 `@playcanvas/splat-transform 3.4.2` 将高斯及 LOD 数据的 LOD 0 解码为独立标准 PLY，粗配准与 ICP 只引用该 XYZ 计算文件。原始 SOG 和 Streamed SOG 通过带会话随机令牌的资源树供 PlayCanvas 按需显示；点云／高斯状态独立。二进制 Gaussian 4 点样本完成 compressed PLY 和 SOG 往返，恢复文件均为 4 点、包围盒 `[0,0,0]～[1,1,1]`；SOG 恢复点与原始点完成 ICP，RMS `0`，正反矩阵为单位阵。服务 84／84、可执行 Web 回归 108／108、Windows CTest 1／1、类型检查、Web 构建及差异检查通过。Docker Desktop 启动未成功，当前 Linux engine pipe 不存在，镜像构建未执行。真实 LCC／LCC2／Streamed SOG 样本及浏览器流式画面仍待验证，不能标记为完整兼容。

- 2026-09-15：按用户反馈将配准结果矩阵从视口底部浮层移至左侧第 4 步下方，指标、业务场景矩阵、原始模型坐标矩阵按单列顺序展示，左侧面板独立滚动；移除旧结果抽屉结构与样式。独立 Chrome 验证两组矩阵顺序、指标位于首项、旧抽屉不存在、展开前后三维视口保持 1140×842 且相机方向不变；左侧结果内容高度 1431 px，可在 842 px 可视区内独立滚动。截图：`output/playwright/workbench-result-column.png`。

- 2026-09-15：修复坐标查询面板打开时导航立方体下移，以及返回配准编辑时相机重置。面板打开后立方体只水平左移，顶部位置保持 72 px；坐标查询进入、配准／原始显示切换、返回配准编辑均不再调用相机适配。独立 Chrome 先主动旋转视角，再验证打开查询、切换原始位置、返回配准编辑全过程导航立方体方向矩阵完全一致；关闭面板后立方体从 x=832 精确恢复至 x=1302，垂直位置始终为 y=72。

- 2026-09-15：按用户反馈将右侧辅助工具面板改为覆盖三维视口的悬浮层，不再占用网格列；面板打开时导航立方体向左、向下让位，关闭后复位。独立 Chrome 在 1440×900 与 1280×900 下确认坐标查询面板宽 460 px，打开前后视口均保持原尺寸，面板、悬浮工具条和导航立方体互不重叠，关闭面板后立方体精确复位。截图：`output/playwright/workbench-overlay-query.png`。

- 2026-09-15：工作台主流程改为左侧纵向连续排列，包含模型与坐标、方向与粗配准、ICP 精配准和结果摘要；辅助工具条悬浮于三维视口左上角，剖切、原点平面、坐标查询在右侧互斥显示，关闭后空间归还视口；结果详情默认关闭并从左侧按需打开。独立 Chrome 在 1440×900 和 1280×900 下验证工具条无横向滚动、主流程入口在工具面板打开时仍可见、三种工具面板切换互斥、右侧关闭后隐藏、结果完成后不自动打开详情、两组矩阵详情可打开及关闭。使用相同 1000 点 PCD 完成合成 ICP，页面显示「配准完成」，浏览器控制台 0 error；类型检查、Web 构建、4 项相关状态／矩阵测试及差异检查通过。首次用沿用的 −90 m 草稿测试配准失败，重置并重新创建相同模型会话后通过；该失败不计入布局结论。

- 2026-09-15：按最新反馈，首页 A／B 业务场景矩阵改为默认收拢，点击标题独立展开或收起，保留输入；提交时若折叠区存在无效输入则展开以显示校验。Chrome 验证默认收拢、切换、保留 −90 及 A／B 独立状态通过，类型检查与 Web 构建通过。

- 2026-09-15：按用户指定原文更新首页业务场景矩阵说明，已核对文案一致；仅修改说明文字，不改变交互逻辑。

- 2026-09-15：首页业务场景矩阵说明补充用途、设置条件及默认使用方式：文件坐标转换到业务场景坐标，模型在场景中有位移、旋转或缩放时设置，直接使用原始坐标时保持默认。类型检查与 Web 构建通过。

- 2026-09-15：按用户反馈移除历史结果「更多」菜单，复制矩阵、继续配准、再保留 24 小时、释放源文件全部直接显示，保留原有禁用条件和释放确认。类型检查、Web 构建与差异检查通过；本轮未执行真实历史操作。

- 2026-09-15：首页数值输入修复首次聚焦后将 `-90` 追加为 `0-90` 的问题。参数及矩阵输入首次鼠标聚焦或 Tab 聚焦时选中原值，再次点击仍可定位光标。Chrome 先复现旧行为，再验证鼠标替换、Tab 替换、矩阵反算提交及再次定位追加；类型检查与 Web 构建通过。

- 2026-09-15：首页保留 A／B 常驻参数及矩阵输入、完整历史操作与访问设置，服务状态接入健康检测。支持负号中间态、矩阵分解回填、Ctrl＋V 与按钮整块粘贴，拒绝无效矩阵且不覆盖有效数据。矩阵专项覆盖负缩放、正负 90° 万向锁、大坐标、格式解析、非仿射／剪切／奇异拒绝；类型检查、Web 构建、差异检查通过。独立 Chrome 在当前 5273 页面验证负号、双向同步、两种剪贴板操作、无效粘贴数据及布局稳定；1280／1440／390 宽度无横向溢出。实际历史为空，本轮未操作真实历史的续期、释放或继续配准；未重跑 ICP。小屏仍需要纵向滚动。截图：`output/playwright/home-compact.png`。

- 2026-09-15（Windows 本地开发端口）：当前 Windows 将 TCP `5141～5240` 保留，默认 Web 端口 `5173` 的最小 Node 绑定返回 `EACCES`。本地开发 Web 端口调整为 `5273`，避免修改 Windows、Docker、WSL 或 Hyper-V 网络配置；API 继续使用 `127.0.0.1:8865`。

- 2026-09-14（重构验收与合并）：用户确认阶段 8 至阶段 12 的重构验收结果；`codex/refactor-registration-workbench` 已 fast-forward 合并到 `main`，并将提交 `d174b5e` 推送到 `origin/main`。合并后工作区干净，`main`、`origin/main` 与重构分支指向同一提交。

- 2026-09-14（ASCII PLY Windows 换行兼容）：阶段 12 验收发现 PowerShell 生成的有效 ASCII PLY 使用 CRLF 时，解析器因签名行残留 `\r` 返回 `Invalid PLY signature`。新增 2 点 CRLF 文件回归先复现失败，再仅规范化签名行结尾；坐标与点数断言通过，既有真实 3777901 点 PLY、149317 点 PCD、4PCS、ICP 和矩阵回归继续通过。Windows CTest 1／1、服务 71／71 和差异检查通过；Windows 预编译 Worker 与清单已更新。

- 2026-09-14（阶段 12 综合验收）：修正候选旋转去重的相对旋转计算，增加验证点少于 40 时的低置信度保护；Windows CTest 以 80 点非对称数据恢复 `120°` 旋转与 `(25，−18，9) m` 位移，并覆盖稀疏证据和相近候选歧义。15% 重叠数据与 `3×3×3` 对称点阵均明确失败，没有生成高置信度矩阵。真实 PLY／PCD 在默认 `delta=0.01 m` 下安全失败；`0.1 m` 候选双向覆盖仅约 1.7%～1.8%，标为 `ambiguous`，`0.25 m` 覆盖约 11.9%～13.5%，标为 `low_confidence`。Docker Engine 29.2.0 构建 `ply-pcd-registration:stage12-acceptance` 成功，容器内合成恢复与低重叠失败边界通过；16 组搜索在 Windows／Docker 分别约 0.63／1.37 秒，均去重为 1 个评分约 0.999993 的候选。Windows 预编译 Worker 与清单已更新；服务 71／71、类型检查和 Web 构建通过。完整证据见 `docs/STAGE12_ACCEPTANCE.md`。

- 2026-09-14（阶段 12 Web 候选确认流程）：工作台增加用户主动触发的 4PCS 搜索、候选评分与风险展示、预览／采用／放弃流程；搜索成功前不改变当前姿态，预览或采用才更新 `moving-local→fixed-local`，失败与放弃保留或恢复原姿态。采用后手调会将 ICP 初值来源标记为 `4pcs_adjusted`，未手调为 `4pcs`，普通手动流程为 `manual`；来源随任务结果写入历史档案。独立服务 `127.0.0.1:8884` 使用 80 点合成会话完成真实页面搜索，得到 1 个评分 1.000、双向覆盖率 100% 的候选；预览恢复 `B→A` 的 −30° 与平移，采用后修改 X 并执行 ICP，任务状态确认 `initial_source=4pcs_adjusted`，最终 RMS 显示为 0.000000 m。联调修复放弃候选恢复姿态后未使既有结果失效的问题。可执行 Web 回归 102／102、专项 2／2、服务 71／71、类型检查、生产构建和差异检查通过；Docker、低重叠、对称歧义、负载及真实数据验收仍属第 5 步。

- 2026-09-14（阶段 12 多候选与风险判定）：Worker 支持最多 16 组 overlap×随机种子组合，按独立双向覆盖率与阈值内 RMS 评分，按平移／旋转距离去除近重复姿态，并输出排序候选和 `none`／`low_confidence`／`ambiguous` 风险。服务限制候选数量与参数边界。合成数据以 overlap `0.8／1.0` 和种子 `42／43` 搜索后收敛为一个去重候选，风险为 `none`、双向覆盖率 1.0；Windows `registration_tests` 1／1、服务回归 71／71 通过。

- 2026-09-14（阶段 12 异步服务任务）：新增会话级 `coarse-register` 提交入口及独立的粗配准状态、取消和结果接口；任务复用现有持久队列、并发信号量、Worker 超时、进程取消、API Key 归属隔离和会话活动锁。重启恢复按 `job_type` 回写正确会话列表，源释放与对象前缀清理覆盖粗配准 Job；普通配准路由拒绝粗配准 Job。隔离服务以两份合成 PLY 完成上传、预览、粗配准轮询和结果读取，HTTP 状态依次为 202／ready／202／succeeded／200，结果双向覆盖率 1.0。服务回归 71／71 通过。

- 2026-09-14（阶段 12 独立 Worker 命令）：新增 `coarse-register-models`，按明确 moving→fixed 方向读取任意两种受支持点云，可应用 A／B 业务场景矩阵，并输出候选矩阵、算法标识、双向覆盖率、阈值内 RMS、组合分数、验证点数、参数和耗时。命令与现有 `register-models` 分离，不触发 ICP，也不改变手动初始矩阵。80 点合成文件端到端恢复 30° 旋转及平移，双向覆盖率 1.0、分数 `0.999992981782`；Windows Release 构建通过。

- 2026-09-14（阶段 12 确定性 4PCS 适配层）：确认当前裁剪的 CCCoreLib 已包含原始 `FPCSRegistrationTools`，无需新增 OpenGR、PCL 或解析依赖。适配层将 moving→fixed 方向显式化，限制输入采样规模，拒绝非刚体结果，并对候选计算独立的双向覆盖率、阈值内 RMS、验证点数和组合分数；上游适配增加非零随机种子入口并记录修改。80 点合成数据在 30° 旋转与三轴平移下恢复候选，固定种子逐元素可复现，双向覆盖率均大于 0.99；Windows Release 构建及 `registration_tests` 1／1 通过。

- 2026-09-14（阶段 11 技术验收）：Web 在当前浏览器会话保存 API Key，并统一接入 JSON、上传、预览、Gaussian、矩阵下载及带鉴权的 SSE 流式进度请求；业务端到端脚本支持从环境变量临时传入 Key。鉴权启用的隔离服务实际验证无 Key 返回 401，带 Key 的 A／B 双角色上传、预览、配准、矩阵下载和历史归档通过，RMS 为 `7.41627e-07`／`5.04159e-07`；Chrome 页面写入 Key 后历史请求返回 200。Web 可执行回归 105／105、服务回归 69／69、类型检查、生产构建、Windows C++ `registration_tests` 1／1 和差异检查通过。生产部署仍需配置实际 Key 摘要，外部 S3／MinIO 联调仍受端点与凭证限制。

- 2026-09-14（阶段 11 服务端鉴权基础）：依据 FastAPI `APIKeyHeader` 与 Python `hmac.compare_digest` 约定，实现可选 `X-API-Key` 摘要认证、用户／管理员身份及资源归属判断；会话创建写入 `owner_id`，Job 和历史归档继承归属，会话、任务、结果、文件与历史路由执行普通用户隔离及管理员放行。缺失／错误 Key 返回带 `WWW-Authenticate: APIKey` 的 401，越权资源返回 404。`tests/service` 共 69 项通过。Web 统一 Header 和 SSE 流式请求尚未完成，阶段 11 继续实施。

- 2026-09-14（阶段 10 生命周期接线）：源文件上传后通过后台线程归档；本地源缺失时，继续配准和会话恢复会从受管对象键下载；成功结果归档后可在本地结果过期时按需回源；立即释放和到期清理删除会话及关联 Job 的远程前缀。对象归档失败返回 502，结果归档失败固定为 `object_storage_failed`。伪造 S3 客户端覆盖上传、原子下载、存在性、分页删除、回滚和结果恢复；`tests/service` 共 63 项通过。默认本地后端隔离合成端到端完成 A／B 两种移动角色，RMS 为 `7.41627e-07`／`5.04159e-07`。外部 S3／MinIO 实例联调未执行，原因是当前未配置端点与凭证。

- 2026-09-14（阶段 10 对象存储基础）：依据 AWS SDK 官方传输与配置接口，引入并锁定 `boto3==1.43.93`。已实现默认本地后端、S3／MinIO 自定义端点与寻址方式、安全对象键、原子下载、存在性检查和分页前缀删除；新增 5 项对象存储测试，`tests/service` 合计 57 项通过，`git diff --check` 通过。上传、恢复、结果归档及保留期清理接线仍在实施中。

- 2026-09-14（阶段 9 任务持久化与队列基础）：`tests/service` 共 52 项通过，覆盖任务描述原子读写、排队任务恢复、运行任务中断收敛、描述缺失／损坏、调度失败、会话缺失隔离、重复提交、取消及 Worker 失败。隔离运行目录 `runtime/stage9-verification` 的合成端到端完成 A／B 两种移动角色，RMS 分别为 `7.41627e-07`／`5.04159e-07`；另以退出码 23 的隔离任务实际重启服务，确认持久 `queued` 任务由启动扫描执行并进入 `failed/worker_failed`。Python 模块编译与 `git diff --check` 通过。

- 2026-09-14（阶段 8 浏览器专项收尾）：同一真实会话完成长方体适配 A／B／全部的画面与六面投影断言；剖切边界同像素复测确认 A、B 不可见侧拒绝且可见侧仍可命中。运行中资源为 1 个画布、1 条 EventSource、10 个带 AbortSignal 的输入监听器；退出后全部归零并确认 EventSource 关闭和监听器中止，重进只有 1 个画布与新监听器。主动改变相机后刷新会话，历史恢复精确回到原适配姿态。页面异常 0。结合已完成的 20 轮 GPU 观测，阶段 8 技术验收项全部完成；随后已由用户确认并合并、推送到 `main`。

- 2026-09-14（三维剖切与 GPU 专项续验）：真实页面逐一拖动 A／B 独立剖切十二个轴向手柄，仅当前模型当前边界变化；旋转长方体六面逐面拖动时对面、相机和粗配准矩阵保持不变；完整 A Gaussian 与旋转长方体及两个原点半空间交集画面通过。连续 20 轮 Gaussian 加载／剖切／释放均成功，常规峰值约 451～455 MiB，第 20 轮释放后 371 个样本在 12.7 分钟内稳定为 272.9 MiB，无逐轮增长。三种长方体适配的同轮视觉确认、剖切边界两侧实际取点和退出后的 EventSource／监听器联合验收仍待补。

- 2026-09-14（进度订阅生命周期收尾）：新增回归先复现旧 EventSource 迟到迭代覆盖当前进度的问题，并覆盖旧 terminal 回调关闭新连接的路径；回调增加当前连接身份校验，已中止控制器禁止重开订阅。专项 5／5、Web 可执行回归 104／104、类型检查及生产构建通过。测试同时覆盖父信号中止后关闭订阅、忽略迟到事件及不再发布页面状态。本轮未执行真实浏览器、Docker 或 GPU 验收，阶段 8 仍进行中。

- 2026-09-14（最新容器复验）：用户手动启动 Docker Desktop 后确认状态 running，Windows 客户端／Linux 引擎均为 29.2.0。镜像 `ply-pcd-registration:verify-current-20260914` 构建成功，容器 `ply-pcd-current-20260914` 使用 `127.0.0.1:8879` 与隔离运行目录 `runtime/docker-current-20260914`，关闭 lifespan 清理。内外健康检查、服务模块导入、合成双角色及真实数据端到端通过，真实 RMS `0.457745013019`；容器 Chrome 页面双尺寸查询锁／面板互斥／矩阵布局通过，页面异常 0，容器重启数 0。镜像包含最新引擎边界调整；三维／GPU 专项不计入通过范围。

- 2026-09-14（Windows／Chrome 续验）：Web 回归 103／103、服务 48／48、Windows CTest 1／1（33.91 秒）、类型检查、生产构建通过。使用独立 Windows 服务 `127.0.0.1:8878` 与 `runtime/stage8-20260914`，关闭 lifespan 清理；合成 A／B 双角色及真实 PLY／PCD 端到端通过，真实 RMS `0.457745013019`，四份矩阵下载与历史归档通过。Chrome／Playwright CLI 验证迟到成功／模拟 503 不覆盖人工姿态、双尺寸查询锁／面板互斥／矩阵布局、联合／独立剖切状态保留、粗配准不变及退出 0／重进 1 画布，页面异常 0。同步修正验收表的旧权限阻塞状态；三维／GPU 未完成项保留，未合并或推送。

- 2026-09-14（引擎依赖边界收尾）：新增静态边界回归先复现 6 处 `engine → app／views` 反向引用，修复后 2／2 通过。资源清理实现迁入 `shared/resource-scope.ts`；剖切启用、编辑模式和既有摘要规则收回 `ClippingStateController.ts`，原应用／视图导出入口保留，未复制规则。60 个可执行 Web 测试文件、类型检查、生产构建和差异检查通过；Vue SFC 查询视图测试仍不计入 Node 数量。
- 2026-09-14：本地 8875 最新构建恢复真实会话，联合／独立模式及 A／B 参数保留、切换编辑对象、工具栏摘要／激活态、关闭面板保留剖切均通过；粗配准矩阵不变，退出画布 0，重进画布 1，页面异常 0。两种尺寸的查询锁／面板互斥／矩阵布局复验通过。此证据不代表三维手柄、Gaussian 交集或长期 GPU 验收完成。

- 2026-09-13（Windows Docker Desktop 当前构建复验）：桌面程序状态 running，Docker Desktop 4.60.0，Windows 客户端及其托管 Linux 引擎均为 29.2.0。当前镜像 `ply-pcd-registration:verify-current-20260913` 构建通过，独立容器 `ply-pcd-current-20260913` 在 8877 运行，重启数 0；服务新模块导入、内外健康检查、合成双角色与真实数据回归通过，真实 RMS `0.457745013019`。容器 Chrome 页面通过历史迟到响应保护、退出画布归零、双尺寸查询锁／面板互斥／矩阵布局。详细证据见 `docs/STAGE8_ACCEPTANCE.md`，剩余三维与 GPU 专项不计入本轮通过范围。

- 2026-09-13（独立 Chrome 复验与结果展示收尾）：服务 48／48、Windows CTest 1／1（32.54 秒）、Web 可由 Node 执行的 59 个测试文件全部通过（显式排除依赖 Vue SFC 的 `coordinate-query-views.test.ts`），类型检查与生产构建通过。修复导航立方体旧测试仍读取迁移前源码路径的问题；结果展示方向、公式和格式化迁入 `registration-result-presentation.ts`，新增 2 项回归。结果抽屉按实际宽度排列矩阵，1440×900 和 1280×720 真机截图与 DOM 尺寸验证通过。
- 本轮隔离本地服务 8875 使用 `--lifespan off`；合成 A／B RMS 为 `7.41627e-07`／`5.04159e-07`，真实 RMS 为 `0.457745013019`。独立 Chrome 验证历史恢复、编辑后迟到成功／503 不覆盖姿态、退出画布数 0、查询锁／只读结果／关闭解锁、面板互斥；页面异常 0。复现脚本和当前会话见 `docs/refactor-integration-regression.md`。Docker 与三维／GPU 剩余项未标完成。

- 2026-09-13（验证方式留档）：按用户要求，将 Windows Docker Desktop、独立 Chrome／Playwright CLI、历史脚本位置及证据边界补入 `docs/STAGE8_ACCEPTANCE.md`，并保存独立记忆更新说明。切回官方配置后内置浏览器仍被管理员策略校验不可用拒绝；不以此判断独立 Chrome 故障。Docker 后续曾停止，服务当前状态需重新检查；本条不新增浏览器或 GPU 验收通过结论。

- 2026-09-13（Windows Docker Desktop 恢复）：使用 `docker desktop start` 启动本机 Docker Desktop 4.60.0，确认客户端 `windows/amd64`、引擎 29.2.0 `linux/amd64`。构建 `ply-pcd-registration:verify-20260913` 成功，独立容器 `ply-pcd-verify-20260913` 绑定 `127.0.0.1:8875`，运行目录为 `runtime/docker-verify-20260913`，关闭启动清理，保持运行供验收。健康与首页 200，新服务模块导入通过，重启数为 0。容器合成 A／B 双角色及真实 PLY／PCD 回归全部通过，真实 RMS 为 `0.457745013019`；Windows CTest 1／1（35.62 秒）、Web 自动回归 98／98 通过。浏览器访问 8865 仍因管理员策略无法校验被拒绝。

- 2026-09-13（本轮续作）：服务回归 48／48、Web 类型检查、生产构建及差异检查通过。隔离端口 8876 的合成 A／B 双角色 RMS 为 `7.41627e-07`／`5.04159e-07`，真实 PLY／PCD 为 `0.457745013019`；正逆矩阵、文件矩阵下载和历史归档均通过。运行目录为 `runtime/refactor-check-20260913`，服务使用 `--lifespan off` 避免实际清理，周期调度另由隔离异步测试覆盖。Docker CLI 29.2.0 可执行，提升权限后 Linux 引擎管道仍不存在；Playwright CLI 0.1.19 可运行，但内置浏览器访问本机页面仍被管理员策略校验不可用阻断，未绕过。

- 2026-09-13：工作台初始化装配迁入 `app/workbench-runtime.ts`，页面仅保留生命周期入口及样式；逐行对比确认初始化函数体和资源登记顺序未变。可由 Node 加载的 Web 回归 98／98、类型检查及生产构建通过，仅有既有 PlayCanvas Worker 外部化和大包提示。依赖 Vue SFC 的 `coordinate-query-views.test.ts` 未用 Node 执行；浏览器联合验收继续按权限阻塞记录，不以构建代替。

- 2026-09-13：继续收敛工作台入口，移除页面中不再使用的模型信息局部变量和移动实体辅助函数，避免残留状态暗示页面继续承担引擎职责。`pnpm run typecheck:web`、工作台生命周期回归和 `git diff --check` 通过；未改变运行时行为。

- 2026-09-13：新增 Gaussian 完整加载／释放 20 轮生命周期回归，确认每轮均创建并移除一个实体与资源，销毁后无重复清理；全量可由 Node 加载的 Web 回归 98／98、Web 类型检查、Windows CTest 1／1、服务回归 38／38 和 `git diff --check` 通过。该回归验证释放路径，不替代浏览器 GPU 显存长期观测。

- 2026-09-13：再次尝试通过 Codex 内置浏览器访问本地工作台时，浏览器自动审批服务返回 503，提示当前分组没有可用的 `codex-auto-review` 渠道；未使用绕过方式，浏览器联合验收和真实 GPU 显存观测继续保持待补状态。

- 2026-09-13：Docker Engine 29.2.0 启动后，Linux 镜像完成 Web 构建及 72 步 C++ Worker 编译；修复容器 Python 包复制层级后，镜像内 `import service.app`、容器健康检查、合成 A／B 双角色端到端和真实 PLY／PCD 端到端通过，真实 RMS 为 `0.457745013019`。验证使用独立宿主端口 8875，完成后已停止临时容器与 Compose 服务，保留镜像和运行数据。

- 2026-09-13：首次检查时 Docker CLI 29.2.0 与 Compose 5.0.2 可执行，但 Docker Engine 尚未启动，且沙箱内用户级 `~/.docker/config.json` 无读取权限；随后经用户授权启动本机 Docker Desktop并完成上条容器验证，未修改系统配置。

- 2026-09-13：`pnpm run test` 使用 `build/windows-release` 中的真实 `registration_tests`，CTest 1／1 通过，最新耗时 35.82 秒；本地运行准备确认继续使用 `runtime/local/bin/registration_worker.exe`。uv 尝试写入用户级 Python 注册表时收到拒绝访问警告，但依赖检查、Worker 选择和测试均成功，不影响本轮验证结论。

- 2026-09-13：全部可由 Node 直接加载的 Web TypeScript 回归 98／98、服务回归 38／38、`pnpm run typecheck:web`、Vite 生产构建及 `git diff --check` 通过；构建仅保留既有 PlayCanvas `worker_threads` 外部化与大包提示。前后端本机健康检查均返回 200；内置浏览器访问本机工作台时因管理员安全策略校验不可用被拒绝，未绕过安全控制，近期输入路由、任务动作、历史恢复和剖切运行时浏览器联合回归待补。

- 2026-09-12：使用 `--experimental-transform-types` 执行全部可由 Node 直接加载的 Web TypeScript 回归，84／84 通过；`coordinate-query-views.test.ts` 直接依赖 Vue SFC，Node 原生加载器不支持 `.vue`，其装配由 Vite 生产构建和真实浏览器覆盖。修复 24 个新增测试入口／内部模块缺失 `.ts` 后缀的问题。`pnpm run typecheck:web`、Vite 生产构建和服务端 38／38 通过。

- 2026-09-12：Playwright 在真实成功结果会话验证历史矩阵与 RMS 恢复；开启坐标查询后入口激活，ICP 与粗配准禁用，剖切仍可用；查询与剖切面板可同时显示，打开原点平面后查询关闭。1440×900、1280×720、900×650、390×844 均无页面横向溢出。离开工作台后画布由 1 个降为 0，再进入仍为单画布并恢复结果，控制台 0 错误、0 警告。重新执行真实 ICP 任务 `e7abd0c5-ed92-4542-ab4d-260cbe9b246d` 成功，耗时约 0.53 秒并写入会话历史。

- 2026-09-12：`pnpm run typecheck:web` 通过；Vite 生产构建通过，保留既有 PlayCanvas worker externalization 与 chunk size 提示；会话加载／上下文、资源生命周期、每帧更新、活动／结果状态、剖切派生状态、坐标查询锁定、原点平面和模型显隐共 18／18 通过。Playwright 使用真实成功结果会话，确认历史矩阵与 RMS 恢复、900×650 页面 `scrollWidth=clientWidth=885`、工作台画布 1 个，点击「新建」后为 0，控制台 0 错误；查询按钮可用，但开启后的锁定状态尚无有效命令结果。

- 2026-09-12：`vue-tsc --noEmit -p web/tsconfig.json` 通过；Vite 生产构建通过；`node --experimental-strip-types --test web/src/stores/workbench-activity.test.ts` 4／4 通过。构建仅保留既有 PlayCanvas worker externalization 与 chunk size 提示。

- 2026-09-12：结果状态新增 5 项回归，连同活动状态、历史恢复、业务矩阵状态共 21／21 通过；任务控制器、资源作用域、坐标数学及显示矩阵回归共 9／9 通过。类型检查、生产构建、差异检查通过。本轮没有启动浏览器或真实 ICP。

## 已完成

- 2026-09-14：完成阶段 11 API 鉴权与工作区访问控制。服务端使用摘要配置和常量时间比较认证 `X-API-Key`，按用户／管理员身份隔离资源；Web 的常规请求、上传、下载、Gaussian 和 SSE 进度流均携带会话级 Key。默认关闭鉴权的本地兼容路径与启用鉴权的双角色端到端均通过。

- 2026-09-14：完成进度订阅连接隔离与退出中止回归，修复关闭／重开进度显示后旧连接回调影响新连接的问题；验证范围为自动回归，不代表浏览器资源联合验收完成。

- 2026-09-13（本轮续作）：补验 `service/session_state.py` 的 8 项会话状态回归；将周期清理调度移入既有 `service/cleanup.py`，应用入口保留启动装配。新增 2 项异步回归覆盖清理后等待、串行循环、取消及失败传播，服务 48／48 和本地合成／真实数据端到端通过；容器与浏览器验收仍单列待补。

- 2026-09-13：完成工作台页面与应用装配边界收尾。会话、引擎、面板、输入与配准运行时统一在 `app/workbench-runtime.ts` 装配，以显式 `WorkbenchHost` 接口接入页面；`pages/generic-registration.ts` 保留进入、退出和异常资源清理。Web 自动回归、类型检查、生产构建与初始化函数体一致性核对通过；实际交互待权限恢复后验收。

- 2026-09-13：坐标查询的预览点命中循环提取到 `engine/tools/coordinate-query-picking.ts`，集中处理模型世界变换、剖切可见性、相机前方深度、屏幕命中半径和最近点选择。新增 2 项回归，验证被剖切的近点跳过、边界点保留、相机后方及半径外不命中和预览数据不变；全量可执行 Web 回归 98／98、类型检查、生产构建和差异检查通过。浏览器中的实际边界像素命中仍需视觉联合验收。

- 2026-09-13：Docker 实测发现服务拆分后镜像仍将 `service/` 内容平铺到 `/opt/registration-service`，与 `service.*` 包导入不兼容，容器循环重启并报 `ModuleNotFoundError: service`。现将包复制到 `/opt/registration-service/service`，Uvicorn 入口改为 `service.app:app`，日志与静态资源路径同步到包内，并在镜像构建阶段增加 `import service.app` 烟雾检查。Compose 配置、服务回归 38／38、Linux 镜像构建、容器健康及合成／真实数据端到端全部通过。

- 2026-09-13：新增 `docs/STAGE8_ACCEPTANCE.md`，将已通过的自动基线、两种桌面尺寸主流程、独立六向手柄、旋转长方体适配、Gaussian 多条件交集、剖切边界取点、退出释放、20 轮 GPU 观测及 Docker 真实数据步骤整理为可执行验收清单；明确每项证据边界和合并前置条件。文档与路线图待办核对、差异检查通过。

- 2026-09-13：原点平面面板状态、PlayCanvas 场景控制器、检查器冲突关闭、工具栏激活态、Gaussian 提示监听和销毁组合到 `app/workbench-origin-plane-runtime.ts`；工作台页面仅挂接运行时端口，不再直接编排原点平面 UI 与引擎生命周期。全量可执行 Web 回归 95／95、类型检查、生产构建和差异检查通过；浏览器联合交互仍随当前管理员安全策略阻塞待补。

- 2026-09-13：统一跨端自动回归通过：服务 38／38、Windows C++ `registration_tests` 1／1；合成业务坐标端到端分别以 A、B 为移动模型完成，RMS 为 `7.41627e-07`／`5.04159e-07`，并验证正逆矩阵、文件矩阵下载与历史归档；真实 `point_cloud.ply`／`GlobalMap.pcd` 以 B 为移动模型完成，RMS 为 `0.457745013019`。本地准备继续选中仓库内 `runtime/local/bin/registration_worker.exe`，无需系统级依赖变更。

- 2026-09-13：加强 `clip-box-fitting.test.ts`，验证旋转 90°、非均匀缩放及平移后的模型八角点精确世界包围盒、A／B 联合范围、输入边界不变和退化轴最小尺寸；专项 2／2、全量可执行 Web 回归 95／95、类型检查、生产构建和差异检查通过。该证据覆盖适配算法，不替代真实三维画面验收。

- 2026-09-13：新增 `gaussian-clipping.test.ts` 并加强 `ClippingSceneController.test.ts`，直接验证完整 Gaussian 接收 A／B 独立原点半空间、旋转长方体世界逆矩阵和轴向范围；验证参数更新按 75 ms 限频、相同静止状态不重复提交、松手强制提交最终状态。专项 3／3、全量可执行 Web 回归 94／94、类型检查、生产构建和差异检查通过；该证据覆盖 CPU 参数链路与 shader 装配，不替代真实 GPU 画面和长期显存验收。

- 2026-09-13：新增 `ClippingSceneController.test.ts`，直接覆盖 A／B 独立轴向剖切六个边界、模型状态隔离、强制 Gaussian 同步参数，以及平移旋转和非均匀缩放后的长方体局部空间内外判断。专项 2／2、全量可执行 Web 回归 93／93、类型检查、生产构建和差异检查通过；该测试证明数学与状态路由，不替代三维手柄和 Gaussian 画面验收。

- 2026-09-13：完成 README、API、实施方案和专项验收记录的一致性核对。`IMPLEMENTATION_PLAN.md` 当前业务章节移除“旧 API 保留”和“Worker 仍在原始文件坐标配准”的过期表述，HTTP 接口清单更新为实际 v2 会话／任务入口；2026-09-07 方案明确标为已替代。剖切与坐标查询记录将已覆盖的联合六向、三轴拖动和相机旋转／缩放从旧待办中核销，保留真实未验项。全文检索与差异检查通过。

- 2026-09-13：完成阶段 8 代码边界审计：Vue SFC 未直接导入 PlayCanvas、操作场景实体或依赖引擎 DOM，`web/src/engine` 未导入 Vue SFC；输入控制器保留画布／窗口事件属于引擎输入职责。修复 `clipping-panel-binding.ts` 为类型推导反向依赖 `app` 运行时的问题，改用只含 `fit` 的最小端口，恢复 `app → views` 单向装配。类型检查、架构检索和差异检查通过。

- 2026-09-13：清理路线图顶部已被后续证据覆盖的旧“待验证”迁移记录，将阶段 8“进行中”收敛为浏览器联合验收、剩余三维专项、最终边界审计、跨端／分发验证和文档一致性五类真实工作；移除日期过期且内容重复的未完成清单，并修正历史区指向。完成证据仍保留在“已完成”和“最近验证”。

- 2026-09-13：角色、粗配准、业务矩阵、ICP 参数和任务操作五个 Vue 插槽的 VNode 构造集中到 `views/workbench/registration-panel-binding.ts`，继续消费既有响应式状态和运行时动作，不复制业务规则。页面只提供场景回调并一次挂接插槽；`generic-registration.ts` 由 310 行降至 289 行。可执行 Web 回归 91／91、类型检查、生产构建和差异检查通过；面板联合交互回归待浏览器安全策略恢复后补验。

- 2026-09-13：模型与剖切 Gizmo 输入仲裁、模型变换事件、四类工具注册、工具激活优先级、查询剖切状态同步及监听销毁集中到 `app/workbench-tool-runtime.ts`。工具管理器使用明确的 `WorkbenchToolId` 泛型，页面仅提供状态查询和场景动作；`generic-registration.ts` 由 318 行降至 310 行。可执行 Web 回归 91／91、类型检查、生产构建和差异检查通过；浏览器模型／剖切手柄切换联合回归待安全策略恢复后补验。

- 2026-09-13：任务请求构造、任务控制器事件、迭代矩阵应用、成功／取消结果处理、执行／取消／过程显示动作及历史恢复组合到 `app/workbench-registration-runtime.ts`。该运行时复用既有独立模块，页面仅绑定 Vue 操作面板；`generic-registration.ts` 由 359 行降至 318 行。可执行 Web 回归 91／91、类型检查、生产构建和差异检查通过；真实任务提交与历史恢复浏览器联合回归待安全策略恢复后补验。

- 2026-09-13：坐标查询视图、屏幕标签、引擎控制器、查询锁、冲突检查器关闭、原始／配准显示切换、剖切可见性判断和销毁集中到 `app/workbench-coordinate-query.ts`。页面仅保留可供工具栏、角色和任务流程调用的查询控制器引用；`generic-registration.ts` 由 376 行降至 359 行。可执行 Web 回归 91／91、类型检查、生产构建和差异检查通过；浏览器查询锁、取点和退出释放联合回归待安全策略恢复后补验。

- 2026-09-13：剖切面板的模式／编辑目标／范围／辅助体／作用模型／适配／清除事件，以及剖切实体显隐、工具栏标题、Gaussian 状态和视口提示刷新，集中到 `views/workbench/clipping-panel-binding.ts`。页面只创建依赖并挂接面板插槽；`generic-registration.ts` 由 403 行降至 376 行。可执行 Web 回归 91／91、类型检查、生产构建和差异检查通过；该薄绑定层直接依赖 Vue SFC，由 `vue-tsc` 与 Vite 编译覆盖，浏览器联合回归待安全策略恢复后补验。

- 2026-09-13：剖切场景控制器、屏幕标签、三维手柄、联合／独立剖切盒初始适配和销毁顺序集中到 `app/workbench-clipping-runtime.ts`，页面只保留面板响应式事件和工具互斥。新增双模型预览边界合并及输入不变性回归；`generic-registration.ts` 由 431 行降至 403 行，可执行 Web 回归 91／91、类型检查、生产构建和差异检查通过。浏览器手柄与标签生命周期联合回归因管理员安全策略校验不可用待补。

- 2026-09-13：最近历史结果对角色、ICP 参数、场景结果、状态和相机的恢复应用迁入 `app/workbench-history-restoration.ts`，页面不再读取历史摘要字段或维护参数映射。新增恢复顺序及未知参数隔离回归；可执行 Web 回归 90／90、服务回归 38／38、类型检查、生产构建和差异检查通过。浏览器历史恢复回归因当前管理员安全策略校验不可用待补。

- 2026-09-13：配准执行、取消和过程显示的界面编排迁入 `app/workbench-registration-actions.ts`，组合复用既有取消与进度动作，页面只提供会话、响应式状态、请求构造及结果状态写入，未形成第二套行为源。新增执行顺序、查询／运行锁定、进度联动及取消失败恢复回归；可执行 Web 回归 90／90、类型检查、生产构建和差异检查通过。内置浏览器因管理员安全策略校验不可用而拒绝本机页面访问，本批浏览器点击与真实任务回归待能力恢复后补验。

- 2026-09-13：视口输入委托迁入 `app/workbench-input-binding.ts`，集中处理坐标查询优先拾取、剖切悬停／拖动、鼠标样式、相机阻断和松开后的 Gaussian 剖切强制同步；页面不再内联输入路由。新增无 DOM 路由顺序回归，并随本轮可执行 Web 回归 89／89、类型检查和生产构建通过。浏览器交互回归因当前管理员安全策略校验不可用待补。

- 2026-09-12：剖切工作台的联合／独立范围、作用范围、编辑目标、模式、辅助体、边界启用、重置和清除状态集中到 `views/workbench/clipping-workbench-state.ts`，`generic-registration.ts` 仅保留 PlayCanvas 场景、手柄与面板事件编排。新增状态隔离回归；可执行 Web 回归 85／85、类型检查、生产构建和差异检查通过。真实浏览器验证联合 X 最小值 `-10.125`、独立 A `1.250`、B 默认值互不覆盖，模式往返保持；清除 A 只重置 A，联合值不变；关闭面板后维持单画布，控制台 0 错误、0 警告。

- 2026-09-12：结果显隐、姿态签名失效、迭代提示、完成提示和新任务重置迁入 `stores/registration-result-state.ts`，页面保留场景矩阵及坐标查询应用。回归发现并修复任务成功但未收到迭代时，切换过程显示将完成提示覆盖为等待提示的问题；完成状态改由成功响应决定，显隐开关保留最终指标。新增 5 项测试，故障用例先失败后通过；状态测试 21／21、相关生命周期与矩阵回归 9／9、类型检查及构建通过。浏览器端联合验收仍待完成，不代表阶段 8 全部验收通过。

- 2026-09-12：最近一次配准结果的加载、业务矩阵匹配及恢复条件迁入 `stores/registration-history.ts`，页面仅保留角色／参数／场景应用。修复历史失败响应在姿态已修改或新任务运行时覆盖状态的问题，回归先失败后通过。新增 6 项历史恢复回归，连同 API 和业务矩阵状态共 19 项通过，类型检查及构建通过。浏览器确认真实历史角色 B／方向 B→A 恢复，延迟成功／503 均不覆盖用户修改的 X＝7.25，等待期间退出画布归零且无页面异常；本轮未重新执行 ICP。

- 2026-09-12：业务场景矩阵草稿、默认值、校验、重复提交保护和保存状态迁入 `stores/business-transform-state.ts`，PUT 请求统一由 API 层处理。取消后的请求／响应解析不再触发场景更新。新增 6 项状态回归与原有 7 项 API 回归、显示矩阵回归、类型检查和构建通过。浏览器模拟请求验证零缩放拦截、等待中锁定、非 JSON 503 恢复、成功应用后的业务原点差及退出画布归零；未改写真实会话参数，未重新执行 ICP。

- 2026-09-12：接续上轮未提交的粗配准检查器样式，分组标题独占一行，XYZ 保持同排。1920／1440／1280 宽度下数值可用宽度均为 62 px，`-123.456` 实测文字宽度约 52.78 px；1280 宽度截图确认负数和三位小数完整可读。类型检查及构建通过。

- 2026-09-12：任务控制器在提交、状态和结果异步边界检查取消，销毁后禁止提交或发布迟到成功；父级取消立即关闭进度订阅，无效状态停止轮询。新增四项回归，两个销毁边界修复前失败、修复后通过；类型检查与构建通过。浏览器模拟缺少任务状态，确认只轮询一次、提示失败并恢复重置／执行按钮，未提交真实计算任务。

- 2026-09-12：剖切手柄屏幕标签迁入 `clipping-labels.ts`，控制器通过类型化接口输出位置与可见状态，不再创建或移除 DOM。类型检查与构建通过；浏览器 +X 手柄实拖将范围 12.689 回写为 11.086，标签总数保持 12，关闭辅助体全部隐藏，退出标签归零，无页面异常。

- 2026-09-12：导航立方体的按钮／拖动／投影监听、CSS 姿态与文字更新迁入界面 `view-controls.ts`；相机控制器只处理相机方向、投影和运动，通过类型化方向回调更新界面。新增无 DOM 控件的相机回归验证方向、旋转、正交／透视缩放；测试、类型检查与构建通过。浏览器验证快速 X 视角、立方体实拖、正交激活及切回透视，模型矩阵不变，退出画布归零，无页面异常。

- 2026-09-11：新增 `RegistrationEngine` 统一应用、场景、相机、四个变换手柄、输入与工具管理的创建／销毁，页面改为调用引擎入口并提供业务委托。类型检查与构建通过；浏览器连续两轮真实会话恢复、查询、长方体切换及退出，每轮观察器／画布／查询和剖切标签均归零，无页面异常。专项模块与界面命令装配仍有页面代码，尚未宣称完整 UI／引擎边界完成。

- 2026-09-11：完整 A Gaussian 长方体 +Z 面裁切、原点正负 X 半空间及切回中心点保留状态通过视觉验证，两轮释放均触发 GPU 删除调用。修复原点剖切状态未计入 Gaussian 提示的问题，浏览器验证启用／清空即时更新和退出；前端 10 文件共 20 项测试、类型检查、构建通过。证据及未覆盖范围见 `docs/clipping-browser-regression.md`。

- 2026-09-11：修正画布适配后重新完成联合轴向六向及长方体六面浏览器实拖。轴向每次仅对应数值改变，长方体每次对侧面保持不动；全部操作未改变模型矩阵和相机姿态。具体数值及覆盖边界见 `docs/clipping-browser-regression.md`，本轮无运行代码修改。

- 2026-09-11：修复 PlayCanvas 默认 KEEP_ASPECT 按窗口重算画布尺寸的问题，应用显式使用 FILLMODE_NONE 跟随视口容器。修复前 1920×1080 下画布高 1080、容器高 732，场景底部被裁切；修复后 1920／1440／1280／390 四种窗口下画布与容器宽高一致，无横向溢出，结果隐藏后同步增高，截图确认场景位于视口内。类型检查、构建通过，此前依赖旧尺寸的三维截图定位需重新测定。

- 2026-09-11：修复 Application 构造中途失败的资源泄漏，键盘、鼠标、触摸、应用及 ResizeObserver 创建后立即登记清理。浏览器注入首个观察器构造异常，修复前残留 1 个 keydown 监听，修复后归零；恢复正常会话与退出画布归零通过，类型检查与构建通过。本项覆盖应用创建成功后观察器失败，不宣称覆盖 PlayCanvas 构造器内部所有异常。

- 2026-09-11：查询标签相机旋转与缩放浏览器回归通过，A／B 标签随点投影移动并保持错开，六项业务坐标与模型矩阵不变；截图确认标签贴近三维点。数值和证据路径见 `docs/coordinate-query-browser-regression.md`，本次无运行代码变更。

- 2026-09-11：场景模块迁移后通过浏览器提交真实 PLY／PCD 业务 ICP，任务 `5698c627-5fe2-4733-a1b0-8af3b340d498` 成功，RMS 0.222236119526、50000 点、30.798 秒，与保留基线业务矩阵逐项一致，双向互逆最大误差 1.43e-12。执行锁定、完成进度及解锁通过，详见 `docs/refactor-integration-regression.md`。核对并修正 README 中旧 PLY 固定／PCD 移动、v1、旧矩阵名、文件局部 ICP 与旧布局说明。Docker Linux 引擎管道不可用，容器验证待环境就绪，未修改系统配置。

- 2026-09-11：点云实体、业务变换层、剖切辅助体及显示包围盒迁入 `RegistrationScene`，场景具有独立根与显式销毁，统一释放自建材质；页面保留装配与业务交互。NullGraphicsDevice 回归验证大原点下初始范围、旋转平移后范围及层级／材质单次释放，类型检查、构建通过。真实会话浏览器验证恢复、适应全部、粗调 X 更新矩阵、重置及返回首页无异常；1440×900 截图确认场景和停靠布局正常。统一输入／工具装配入口仍待收敛。

- 2026-09-11：修复 Gaussian 控制器退出后迟到加载成功的资源遗漏：销毁后收到成功事件会再次卸载刚到达的资源，不重新挂载实体或发布界面状态。基于本地 PlayCanvas 2.21.3 Asset／AssetRegistry 源码建立失败回归，修复前资源销毁次数为 0，修复后为 1；Gaussian 三项测试与类型检查通过。此项为资产事件生命周期验证，不代表完整网络取消或长期显存泄漏测量。

- 2026-09-11：模型与长方体两套平移／旋转输入仲裁收敛到引擎 `TransformGizmoInput`，页面读取悬停状态并注册销毁，不再维护重复事件状态。新增事件回归覆盖平移优先、空白禁止旋转、拖动锁定、中／右键禁用及销毁解除监听；测试、类型检查与构建通过，浏览器真实会话恢复、长方体开关和退出无页面异常。本轮事件测试不替代三维手柄逐方向实拖验收。

- 2026-09-11：模型角色说明、初始矩阵和视口操作提示改为响应式视图状态，由 Vue 渲染；新建按钮改为组件事件，移除页面对应 DOM 查询、文本写入及手工监听。类型检查与构建通过；浏览器真实会话恢复后显示四行矩阵及正确移动／固定角色，切换长方体后提示同步更新，新建返回首页并清空画布，无页面异常。

- 2026-09-11：工作台接入主应用 Vue 组件树，移除子应用挂载及加载页 HTML 写入，路由统一管理加载、错误和卸载；按路径重新挂载以避免切换会话复用旧场景。类型检查、构建、前端 8 文件共 17 项测试、服务 38 项测试、Release CTest 1／1 通过。浏览器验证真实会话→模拟失效会话→后退恢复→首页，画布数依次为 1／0／1／0，应用根始终为 1，无页面异常；模拟 404 有预期网络错误。注入第二个 ResizeObserver 初始化失败后，已有观察器归零且画布移除。本轮未新跑真实 ICP，容器及三维专项验收仍待完成；构建仍有 PlayCanvas worker_threads 外部化和大包提示。

- 2026-09-11：业务矩阵、角色、粗调、ICP 参数、执行、剖切、原点与查询视图统一使用外壳类型化插槽，移除八处独立 Vue 应用及其手工卸载，工作台由一个组件树管理。类型检查、构建与浏览器真实会话恢复、粗调 X 变更更新矩阵、范围／姿态切换保留、原点表单及退出验证通过；工作台宿主内额外 data-v-app 根数量为 0，控制台无错误／警告。

- 2026-09-11：检查器面板显隐改用 `InspectorState`，场景互斥依据状态计算，不再读取 DOM hidden；Vue 负责工具显隐与配准表单恢复。剖切最小／最大数值改为分行布局以提供完整输入宽度，同时修复顶部新建按钮低对比度。类型检查、构建与浏览器连续原点／查询／查询叠加剖切／关闭／恢复配准验证通过，截图确认输入完整显示，测得数值区宽 187 像素；退出无运行错误。

- 2026-09-11：结果矩阵组件并入外壳组件树，结果显隐、任务文本、迭代提示、完成样式及进度位置统一使用 `ResultViewState`；新任务启动隐藏旧结果，避免把上次结果当作本次输出。类型检查、构建、浏览器历史恢复／剪贴板一致性／重置失效／过程显隐通过；拦截任务接口与 SSE 验证运行中锁定、迭代文本、完成提示及解锁，最终矩阵读取已有真实结果，本轮没有新运行 Worker。

- 2026-09-11：工具栏整合为 `ViewportToolbar.vue`，模型显隐、原点轴、原点平面、剖切、查询及重置锁定统一使用响应式状态与类型化命令；移除页面和原点面板对工具栏按钮的查找、事件绑定及样式操作。类型检查、构建及浏览器回归通过：显隐／原点激活态、查询锁定与解锁、查询中适应全部、剖切激活态、重置得到单位矩阵并使旧查询失效、退出清除画布。本轮未重复完整 Gaussian 加载或提交新 ICP。

- 2026-09-11：Gaussian 控制器移除 DOM、按钮事件和样式操作，改为公开切换命令及类型化状态回调；Vue `GaussianControls.vue` 管理按钮禁用、激活、文案及标题，提示通过外壳响应式渲染。补充两项无 DOM 回归，覆盖不可用／重复加载与销毁后的迟到错误。类型检查和构建通过；浏览器模拟加载失败后恢复，真实 256,898,154 字节 A PLY 成功显示 Gaussian，X 最大范围设为 −2 后切面可见，切回中心点保留剖切且结果文本不变，观测到 8 次 deleteTexture、2 次 deleteBuffer 调用；该证据不等同于长期显存泄漏测量。

- 2026-09-11：三个工具面板改为 Vue 外壳内的固定检查器宿主，打开时收起配准表单，关闭后恢复，查询与剖切可同时显示；取消基于视口工具栏高度的面板定位。900 像素以下改为纵向模型／视口／检查器／结果布局，并修复旧侧栏绝对定位造成的遮挡。类型检查和构建通过；Playwright 验证 1280 下三面板边界均在视口外、切换及查询返回解锁，390／900 下模型与视口不重叠、无页面横向溢出，关闭工具和执行按钮可达，退出移除画布。截图检查通过，未启动 ICP，完整 Gaussian 专项仍待验收。

- 2026-09-11：新增 `WorkbenchLayout.vue`，直接生成顶部栏、模型侧栏、视口、配准检查器、结果区域及工具栏五组控件；移除页面工作台字符串模板和外壳／工具栏 DOM 重排，原挂载入口改为 Vue 挂载并登记卸载。类型检查、Web 构建通过；Playwright 验证真实会话与历史结果恢复、A 显隐按钮、正交切换、查询锁定、剖切面板开启、退出画布移除与再次加载单实例，控制台无错误。1440×900、1280×720 截图已检查且无页面横向溢出；1280 下工具面板仍覆盖视口，停靠布局未完成。本轮未执行新 ICP 或完整 Gaussian 加载。

- 2026-09-11：工作台初始化与生命周期入口分离，已创建的组件、控制器、Gizmo 和观察器立即登记到资源作用域；初始化失败和页面退出统一逆序清理，单项清理失败仍继续释放其他资源。历史结果返回后增加取消检查，退出无需等待结果请求结束。资源作用域与加载回归 10／10、类型检查、构建通过；Playwright 验证正常／查询开启时退出、结果请求挂起时退出，观察器均归零且无控制台错误；注入第二个观察器构造失败后显示错误页，已有引擎观察器释放且画布移除。本轮验证不等同于所有第三方构造器内部异常或完整 Gaussian 显存释放验收，声明式外壳、停靠布局和统一状态仍待推进。

- 2026-09-11：会话就绪轮询迁入 API 层，HTTP 错误、缺少状态及预览失败停止加载；轮询等待支持立即取消，预览加载失败取消并行请求并解除外部取消监听。过渡宿主检查异步导入后的卸载状态，失败提示改为 Vue 文本渲染并提供返回首页。新增 7 项回归、类型检查、Web 构建通过；Playwright 模拟 API 404 验证只请求一次、HTML 错误文本不生成元素及返回首页，恢复真实会话后画布与历史结果正常。本轮未重跑 ICP；场景创建中途失败的完整资源回收、声明式外壳和停靠布局仍待完成。

- 2026-09-10：业务场景矩阵 A／B 表单、折叠区、应用／恢复默认和提示整合为 `BusinessTransformPanel.vue`，使用响应式状态管理禁用和请求进度，应用期间防止面板重复提交和编辑。类型检查、Web 构建及 Playwright 的零缩放拦截、默认值恢复、请求载荷、等待中锁定、模拟 HTTP 500 后恢复和查询锁定／解锁通过；本轮未向后端保存参数，成功应用及 ICP 链路仍待整体验收。

- 2026-09-10：方向、移动模型选择与范围风险提示迁入 `RegistrationRoles.vue`，请求构造和历史恢复读取响应式状态，运行／查询锁定通过组件属性管理。类型检查、Web 构建及 Playwright 验证通过：查询禁用／返回解锁、移动 A／B 风险提示变化、自动推荐、角色变化不改输出方向并使旧查询失效。本轮未重新提交 ICP；已在顶部核对当前剩余事项。

- 2026-09-10：剖切模式、设置、范围和操作整合到 `ClippingPanel.vue`，十个分散 Vue 挂载收敛为一个；面板标题、关闭、联合／独立分组及方式显隐统一由组件管理，页面保留场景同步与类型化事件处理。类型检查、Web 构建与 Playwright 的范围保留、编辑模型保留、适配 B 点击、清除 B 不影响 A、关闭及重入单实例验证通过。本轮未重复三维手柄实拖与 Gaussian 视觉验收。

- 2026-09-10：剖切范围重置、长方体适配和清除按钮迁入 `ClippingActions.vue`，通过事件调用现有范围及长方体逻辑，按当前方式显示操作。类型检查、Web 构建和 Playwright 验证通过：联合重置、适配 A／B／全部、独立适配 A、清除 A 保留 B 数值、重置 B 关闭边界，返回首页无页面异常。适配包围盒精度与长方体手柄视觉仍待专项验收。

- 2026-09-10：A／B 独立 +X 剖切手柄回归通过：分别编辑 B 和 A 时仅对应范围回写为 10.516／9.429，另一模型及联合范围、模型初始矩阵不变。A 最小 X 设为 9 后向内拖动最大边界，最终限制在 9.000；切回 B 及关闭面板保留配置。记录见 `docs/clipping-browser-regression.md`，本轮未改运行逻辑，其余方向和 Gaussian 视觉仍待验证。

- 2026-09-10：联合轴向 +X 手柄与 Vue 范围表单双向同步实测通过：拖动将最大值 12.689 回写为 9.975 并启用边界，模型矩阵不变；表单改为 5 后手柄标签位置更新，最小／最大限界及重置通过。记录见 `docs/clipping-browser-regression.md`。本轮未改运行逻辑，其余方向、独立手柄和实际剖切画面仍待验收。

- 2026-09-10：联合及 A／B 独立轴向范围迁入 `AxisRangeForm.vue`，使用纯 TypeScript `axis-range.ts` 状态与边界更新函数，渲染／手柄不再读取输入 DOM；范围编辑自动启用对应边界，保留对侧启用时的限界和三位小数。空值及非有限值不覆盖有效范围。类型检查、Web 构建、边界单元回归和 Playwright 的限界／重复越界、空值恢复、模型隔离及重置通过。本轮未实拖三维剖切手柄，手柄双向同步及实际剖切视觉仍待专项验证。

- 2026-09-10：联合及 A／B 独立剖切方式、作用模型和辅助体开关迁入 `ClippingSettings.vue`，剖切控制器直接消费响应式状态，清除操作同步表单。辅助体开关与方式选择集中排列，范围和适配按钮保留原逻辑。类型检查、Web 构建与 Playwright 验证通过：联合长方体／仅 B／关闭辅助体在切换后保留，独立 A 轴向与 B 长方体互不覆盖，清除 B 后 A 配置保持，清除联合同步关闭方式。实际辅助体显示与 Gaussian 剖切视觉仍待专项验证，轴向范围输入仍待迁移。

- 2026-09-10：联合／独立剖切切换及编辑 A／B 按钮迁入 `ClippingControls.vue`，使用响应式状态与类型化事件调用现有剖切控制器，并提供 aria-pressed 状态。类型检查、Web 构建与 Playwright 验证通过：联合／独立切换保留 A 轴向及 B 长方体配置和编辑 B 选择，关闭面板保留剖切激活态，返回首页卸载组件。具体剖切方式、范围和辅助体表单仍待迁移。

- 2026-09-10：真实数据查询点 X／Y／Z 三轴拖动分别通过，三次 A／B 坐标均更新且模型初始矩阵不变；最终坐标对使用服务正反矩阵校验的最大绝对误差为 5.80e-12。中键相机平移后标签随点移动，六项坐标及模型矩阵不变。证据补入 `docs/coordinate-query-browser-regression.md`；本轮未修改运行逻辑，相机旋转／缩放和剖切边界仍待专项验证。

- 2026-09-10：新增 `InputController.test.ts` 自动回归，验证手柄事件传播与取点消费的区别、相机旋转／平移、拖动抑制、释放结束导航、滚轮和销毁监听清理。`node --experimental-transform-types web/src/engine/core/InputController.test.ts` 与 Vue 类型检查通过。本轮仅添加测试和验证说明，未修改运行逻辑；Node EventTarget 验证不替代浏览器捕获／冒泡及 PlayCanvas 集成检查。

- 2026-09-10：真实数据 B 场景取点成功；手柄回归发现查询悬停事件被输入层吞掉，已将拦截条件从消费 pointerdown 改为只阻止相机导航。类型检查、Web 构建通过；1920×1080 浏览器平面手柄拖动后 A／B 坐标同步更新，粗配准初始矩阵保持不变。证据见 `docs/coordinate-query-browser-regression.md`，多轴拖动与标签运动视觉仍待验证。

- 2026-09-10：完成保留真实 PLY／PCD 会话的模型 A 场景取点浏览器回归：可见点命中后生成 A／B 业务坐标并显示两侧标签；隐藏 A 后点击不改写已有坐标；启用坐标轴剖切编辑时取点禁用，关闭面板后恢复；清除点隐藏两侧标签。本轮只验证并记录结果，未修改运行逻辑。操作、观测值和未覆盖范围见 `docs/coordinate-query-browser-regression.md`；三维手柄拖动、B 取点及相机运动标签视觉仍待验证。

- 2026-09-10：坐标查询的点／轴屏幕标签 DOM 创建、位置写入和销毁迁入 `coordinate-labels.ts`，控制器仅输出投影位置；剪贴板写入由界面接口提供，控制器不再创建或查询 DOM，也不直接访问剪贴板。类型检查、Web 构建与 Playwright 验证通过：复制 A 坐标内容为 `7.25 0 0`，清除点后点标签隐藏，离开页面标签清空，重入保持十个标签。标签投影视觉位置、实际三维取点及拖动仍待专项回归。

- 2026-09-10：坐标查询面板的创建、Vue 挂载、样式及销毁移到界面层，由工作台装配；查询控制器依赖纯 TypeScript 面板状态／显隐接口，并提供编辑方、坐标和操作命令，不再导入界面组件装配模块。类型检查、Web 构建及 Playwright 的坐标更新、编辑方只读切换、关闭、首页卸载和重入单实例检查通过。屏幕标签 DOM 与剪贴板仍在控制器中，实际三维取点／拖动仍待专项回归。

- 2026-09-10：视口工具栏分组、查询／原点轴按钮监听及激活样式迁入 `viewport-toolbar.ts`，工作台负责装配；查询控制器通过命令与入口状态接口连接工具栏，销毁时中止界面监听。类型检查、Web 构建及 Playwright 的五组工具栏、A／B 原点开关独立状态、查询开关、首页返回后重新挂载检查通过。查询控制器仍包含面板和屏幕标签装配，尚未完成全部 DOM 解耦；三维实际取点与拖动仍待回归。

- 2026-09-10：坐标查询按钮、XYZ 输入、复制及提示统一迁入 `CoordinatePanel.vue`，控制器通过面板状态和事件连接查询逻辑，移除面板内部 DOM 查询与按钮监听。类型检查和生产构建通过；独立 8866 验证服务上的 Playwright 检查覆盖空点禁用、位置切换、剪贴板坐标对内容、切换编辑方、清除、取点提示和返回编辑。三维实际取点、拖动及控制器外层工具栏解耦仍待完成。

- 2026-09-10：坐标查询 A／B 的 XYZ 输入迁入 `CoordinateFields.vue`，界面装配集中在 `coordinate-fields.ts`，通过数值事件调用现有坐标换算，移除查询控制器对输入值和只读属性的直接操作；销毁时卸载组件。类型检查、Web 构建及 Playwright 验证通过：A 输入 12.5 后 B 显示换算值、非编辑方只读且不禁用、切换编辑方、空值提示和清除点归零。查询按钮、提示及完整面板装配仍待迁移；本轮未验证三维拖动和场景取点。

- 2026-09-10：新增 `web/src/origin-planes.test.ts`，使用 PlayCanvas NullGraphicsDevice、真实实体和 RenderComponent 验证六个正负半空间、原点边界保留、A／B 隔离、平移旋转及非均匀缩放后的多平面交集、模型显隐恢复、清空状态和销毁监听／实体清理。`node --experimental-transform-types web/src/origin-planes.test.ts` 与 Vue 类型检查通过。本轮仅添加回归测试，未修改运行逻辑；该测试不渲染 GPU 画面，真实点云及 Gaussian 剖切视觉验收仍待完成。

- 2026-09-10：进一步分离原点平面界面与渲染控制器：状态契约移入纯 TypeScript 文件，Vue 装配、工具栏激活态和面板生命周期移入界面层，`OriginPlaneController` 不再依赖 Vue 或 DOM，仅消费状态并管理平面及坐标剖切。类型检查与生产构建通过；Playwright 验证 A／B 独立状态、关闭保留设置、全部关闭、返回首页卸载及重新进入仅挂载一个面板，页面异常为零。实际点云与 Gaussian 剖切视觉专项回归仍待完成。

- 2026-09-10：原点平面面板迁入 `OriginPlaneForm.vue`，通过类型化事件更新 A／B 显示与半空间剖切状态；控制器渲染和剖切查询改为直接读取状态，移除每帧 DOM 表单查询，销毁时卸载组件并移除按钮监听。Vue 类型检查、Web 构建及 Playwright 的模型独立编辑、关闭再打开保留设置、全部关闭和工具栏激活态验证通过。控制器暂时负责组件装配；点云及 Gaussian 实际剖切视觉效果仍待专项回归。

- 2026-09-10：A／B 业务场景矩阵的平移、旋转、缩放及只读预览迁入 `BusinessTransformForm.vue`，页面使用响应式草稿接收编辑，运行／查询锁定通过属性传入，离开页面时卸载组件。Vue 类型检查及 Web 构建通过；Playwright 验证单模型编辑不影响另一模型、平移空值按零预览、重置恢复两模型默认参数、零缩放阻止提交且未发送 PUT。保留已有应用接口和校验；本轮未重新验证有效参数保存及完整 ICP 流程。

- 2026-09-10：粗配准平移／旋转六项输入迁入 `CoarsePoseForm.vue`，通过数值事件调用显示控制器；非编辑字段随模型姿态刷新，运行和查询状态通过属性锁定。Vue 类型检查及 Web 构建通过；Playwright 实测平移 X 输入 1.25 后矩阵对应更新，重置后表单恢复 0.000。三维手柄拖动与完整多轴交互仍待专项验收。

- 2026-09-10：完成保留会话的大体积数据浏览器验收，输入为 3,777,901 点 PLY 与 149,317 点 PCD。运行中终止后服务端返回 cancelled，参数及执行按钮恢复；同会话重新执行成功，RMS 0.222236 m、匹配 50,000 点、耗时 27.16 s。四份结果矩阵正常显示，刷新恢复成功；业务正反矩阵乘积最大误差约 1.43e-12，文件坐标正反矩阵约 4.42e-16，下载值与结果 JSON 一致，浏览器控制台 0 错误／0 警告。验收任务：61d25164-2221-4773-9513-b65a5f2824a8，终止任务：719b611b-3515-4358-9e10-420629d4a4a1。

- 2026-09-09：Playwright 实机验证最近迁移的参数、任务控件和结果组件：历史结果恢复、参数编辑、业务矩阵剪贴板内容、查询锁定／返回解锁均通过。保留的 1000 点测试会话完成真实 Worker 配准，采样上限 60000 正确提交，运行中禁用参数且允许过程显示，完成后展示结果并恢复编辑。1440×900 和 1280×720 无页面横向溢出，返回首页移除组件，控制台 0 错误／0 警告。验证服务关闭启动清理；尚未覆盖大体积数据完整验收与运行中终止点击。

- 2026-09-09：执行、终止和过程显示控件迁入 `RegistrationActions.vue`，通过事件调用现有任务控制器，运行、查询锁定和终止中状态改为响应式属性；运行期间过程显示仍可切换。Vue 类型检查、Web 构建及空闲／运行／终止中／查询锁定 SSR 渲染检查通过；浏览器点击与完整任务交互仍待实机验收。

- 2026-09-09：ICP 四项数值参数迁入 `IcpParameters.vue`，使用响应式参数保存用户输入、构造提交请求和恢复历史值；配准运行及坐标查询锁定通过组件属性控制禁用，任务按钮仍由现有控制器管理。Vue 类型检查、Web 构建和 SSR 历史值／禁用状态渲染检查通过；浏览器输入与完整提交流程仍待实机验收。

- 2026-09-09：结果矩阵和指标展示迁入 `RegistrationResultPanel.vue`，通过类型化响应式属性接收结果及输出方向，正反业务矩阵、原始文件矩阵和复制反馈由组件管理；卸载时清理反馈计时器。工作台保留结果有效性和外层显隐控制。Vue 类型检查、Web 构建、SSR 两个方向及缺少文件矩阵的渲染检查通过；浏览器布局与剪贴板交互仍待实机验收。

- 2026-09-09：历史归档、源文件释放与历史视图迁入 `service/history.py`，过期清理迁入 `service/cleanup.py`；应用层保留动态依赖包装，历史文件格式和清理规则不变。新增运行任务及非 UUID 目录保护、过期结果处理和活动会话源文件保护回归，服务测试 38／38、Python 编译及差异检查通过。新增清理测试模拟删除操作，不触碰真实运行数据。

- 2026-09-09：会话上传创建接口迁入 `service/routes/uploads.py`，集中处理双模型格式与参数校验、流式保存、输入摘要及预览启动；应用入口负责依赖装配。新增成功、空文件和非法参数回归，服务测试 36／36、Python 编译及差异检查通过。当前会话创建、配准启动和任务查询接口均已迁出应用入口。

- 2026-09-09：经用户确认停止提供 v1 API 和旧版手工配准页面，移除对应前后端入口、请求模型和旧客户端示例。新版任务状态、SSE、取消、结果与下载统一迁到 `/api/v2/registrations`；读取历史状态时规范化旧任务链接，不改写历史矩阵档案。规范、README 和当前 OpenAPI 对应的 API 文档已同步。服务测试 33／33、Vue 类型检查、Web 生产构建、Python 编译及导航立方体回归通过；本轮未执行真实数据浏览器端到端验收。

- 2026-09-09：v2 配准启动接口迁入 `service/routes/registrations.py`，注入共享提交锁、状态存储、源文件检查及后台启动回调。新增重复提交只启动一次、业务坐标双矩阵与进度参数、缺失源文件和无效方向回归，服务测试 31／31、Python 编译及差异检查通过。会话上传创建、旧版启动接口和前端组件化仍在待办范围。

- 2026-09-09：v2 会话预览恢复与预览文件接口迁入 `service/routes/sessions.py`，后台启动通过注入回调接入统一任务登记。新增重复恢复、就绪预览、源文件缺失、工作区隔离及预览文件映射回归，服务测试 27／27、Python 编译及差异检查通过。会话上传创建和配准启动接口仍待迁移。

- 2026-09-09：后台任务登记统一到 `service/background_tasks.py`，清理循环、两类预览和配准启动共 7 处调用复用任务创建、强引用保留及完成回收逻辑。新增等待中保留、成功／失败／取消后回收测试，服务测试 22／22、Python 编译及差异检查通过；任务并发和子进程生命周期继续由已有运行器与共享信号量管理。

- 2026-09-09：任务状态、进度 SSE、取消、结果读取和下载接口迁入 `service/routes/jobs.py`，由应用注入存储、会话同步与共享运行进程表。新增 SSE 全量／最新回放及终态、重复取消、完成任务拒绝取消、下载白名单和 HTTP 冲突响应回归，服务测试 19／19、Python 编译及差异检查通过。后台任务登记和剩余会话创建／注册启动接口仍待拆分。

- 2026-09-09：配准 Worker 子进程运行、进度记录、结果矩阵处理和终态同步迁入 `service/registration_tasks.py`；应用层按调用注入状态函数、共享信号量与进程表，保留旧版测试和取消接口的衔接。新增成功、非零退出、缺失结果、取消优先及超时回收 5 项回归测试，服务测试 14／14、Python 编译检查通过；ASGI 集成验证健康检查 200 与缺失任务 404。共享并发对象、后台任务登记及取消／SSE 路由仍待后续拆分。

- 2026-09-09：v1 手工预览与 v2 双模型预览的子进程执行、超时和结果状态处理合并到 `service/preview_tasks.py`，应用层保留 `_run_preview`／`_run_model_preview` 兼容包装并动态注入目录、状态和并发依赖。`service/app.py` 由 1003 行降至 935 行；Python 编译检查、服务测试 9／9，以及两类模拟 Worker 成功路径和 URL 映射检查通过。

- 2026-09-09：v2 会话状态、业务矩阵更新、源文件保留和释放 API 迁入 `service/routes/sessions.py`，共用会话归属校验并通过 Router 工厂注入存储、校验、历史与清理依赖；后台预览恢复和注册启动暂留应用层，等待任务服务拆分。`service/app.py` 降至 1003 行；Python 编译检查、服务测试 9／9，以及状态读取、矩阵更新、保留期限和释放路由注册检查通过。

- 2026-09-09：C++ Worker 的注册、通用模型注册、两类预览、点云检查和矩阵求逆执行逻辑统一迁入 `worker_commands.hpp／cpp`；`main.cpp` 由 520 行降至 58 行，只保留命令分派与统一异常退出。Windows Release Worker 已重建，CTest 1／1 通过；CLI 黄金矩阵求逆、真实 PCD `149317` 点检查及空参数退出码 `10` 冒烟通过。

- 2026-09-09：v2 配准历史列表与详情 API 迁入 `service/routes/history.py`，通过 Router 工厂注入历史目录、记录路径、视图和工作区校验依赖；保留 `workspace_id` 查询参数、完成时间倒序、损坏记录跳过和缺失记录 404 行为。`service/app.py` 降至 1038 行；Python 编译检查、服务测试 9／9 及路由签名和响应检查通过。

- 2026-09-09：C++ Worker 的矩阵 JSON、紧凑进度矩阵和点云摘要输出迁入 `worker_output.hpp／cpp`，`main.cpp` 由约 565 行降至 520 行，只保留命令流程编排和调用。Windows Release Worker 已重建并刷新预编译文件与源码指纹，CTest 1／1 通过。

- 2026-09-09：普通文件上传与带 SHA-256 的流式上传迁入 `service/uploads.py`，继续采用固定分块写入并在完成后关闭 `UploadFile`；应用入口只保留调用，服务端测试 9／9 和 Python 编译检查通过。

- 2026-09-09：FastAPI 健康检查、首页、手工配准页和 v2 会话 SPA 页面迁入 `service/routes/web.py`，通过 Router 工厂注入会话目录及状态读取依赖，应用入口只负责装配。服务测试 9／9 和 Python 编译检查通过；真实 HTTP 验证 `/health`、首页、有效 v2 会话返回 200，无效会话返回 404。

- 2026-09-09：FastAPI 的 Job／会话／历史路径解析、工作区 UUID 校验及状态文件原子读写迁入 `service/storage.py`。`service.app` 通过当前 `RUNTIME_ROOT` 调用存储模块并保留原包装函数，因此临时运行目录注入、既有测试猴子补丁和 API 行为不变；Python 编译检查及服务端测试 9／9 通过。

- 2026-09-09：坐标查询实现迁入 `web/src/engine/tools/CoordinateQuery.ts`，工作台直接依赖引擎工具路径，原 `web/src/coordinate-query.ts` 保留兼容导出。真实历史结果验证 A 坐标 `(3,4,5)` 双向换算、A／B 编辑点切换、原始／配准位置切换、关闭查询和离开工作台生命周期；Vue 类型检查、Web 生产构建通过，浏览器控制台 0 错误、0 警告。

- 2026-09-09：剖切场景计算迁入 `ClippingSceneController`，统一维护联合／独立轴向边界、剖切盒世界逆矩阵、中心点与 Gaussian GPU 参数、坐标取点可见性和辅助盒边线；页面只提供界面输入与控制命令。Vue 类型检查及 Web 生产构建通过，真实历史会话验证联合 X 最小边界、Gaussian 加载／同步／释放，浏览器控制台 0 错误、0 警告。

- 2026-09-09：C++ Worker 的参数结构、用法文本和四类命令解析从 `main.cpp` 迁入 `worker_arguments.hpp／cpp`，共用缺失参数读取逻辑并保持原参数、默认值、校验和错误文案。`main.cpp` 由约 776 行降至 565 行；Windows Release Worker 重建并刷新预编译文件与源码指纹，CTest 1／1 通过，空参数用法及缺失参数退出码冒烟检查通过。

- 2026-09-09：FastAPI 的业务坐标矩阵运算迁入 `service/transform_math.py`，业务预变换、ICP 参数、刚体矩阵和上传扩展名校验迁入 `service/validation.py`。`service.app` 保留兼容别名，现有测试和内部调用入口无需调整；Python 编译检查与服务端 9／9 测试通过。

- 2026-09-09：首页新任务草稿正式接入 Pinia，统一保存 A／B 文件、业务坐标预变换、输出方向、ICP 移动模型、上传进度和错误状态；`HomeView` 只负责界面和路由。工作台「新建」改用 Vue Router 返回首页，草稿在首页与工作台的 SPA 路由切换间保留。Vue 类型检查、Web 生产构建通过；浏览器实测 B→A／移动 A 设置往返工作台后保持，控制台 0 错误、0 警告。

- 2026-09-09：完成正式重构第二批引擎职责拆分。完整 Gaussian 的按需加载、中心点显隐、GPU 剖切同步、资源释放和界面状态迁入 `GaussianDisplayController`；联合／独立剖切模式、当前编辑模型、辅助体显隐、启用状态和摘要迁入 `ClippingStateController`，页面脚本不再保存重复的剖切状态。真实历史会话验证 A 坐标轴与 B 长方体可同时启用，编辑模型和帮助提示同步；A Gaussian 可在独立剖切下加载、同步并释放。Vue 类型检查、Web 生产构建和浏览器控制台检查通过。

- 2026-09-09：完成正式重构第一批基础设施。Web 入口迁移到 Vue 3、Pinia 和真实 Vue Router，首页改为组件化双模型任务创建与历史面板；v2 工作台采用顶部应用栏、左侧模型流程、中央 PlayCanvas 视口、右侧配准检查器和底部结果抽屉，1440×900 工具栏无需横向滚动，1280×720 核心区域可达。PlayCanvas 已提取 `RegistrationApplication`、`InputController`、`ViewportCameraController`、`ToolManager` 和 `RegistrationJobController`，页面卸载会终止请求、SSE、观察器、Gizmo、控制器和应用实例。FastAPI 已提取配置与请求 Schema，并增加可直接刷新的 `/registration/{session_id}` 页面入口；旧 `/?session=...&api=v2` 地址继续兼容。Vue 类型检查、Web 生产构建、9 项服务测试、真实历史会话路由恢复及浏览器控制台检查通过。

- 2026-09-08：通用配准结果同时展示「业务场景转换矩阵（含预设）」与「原始模型坐标转换矩阵」。业务矩阵继续按 `M_business_a_to_b = P_b × M_file_a_to_b × inverse(P_a)` 作为主结果和坐标查询依据，原始矩阵用于文件坐标换算；两套矩阵均显示当前 A／B 方向、坐标公式、反向矩阵和独立复制按钮。真实历史结果页面确认两套数值不同且标识清晰，业务矩阵组合测试 `5／5`、类型检查和 Web 构建通过，浏览器控制台无错误。

- 2026-09-08：坐标查询的非编辑方换算结果由禁用输入框改为只读输入框，保持正常文字对比度和文本光标，支持鼠标框选及 `Ctrl+C` 复制，同时继续阻止直接修改。浏览器实测可将完整长小数选中、复制并粘贴，控制台无错误，类型检查和 Web 构建通过。

- 2026-09-08：修复 Windows 本地开发启动时 API 端口落入系统 TCP 排除范围导致 `WinError 10013` 的问题。默认 API 端口由 `8765` 调整为 `8865`，同步本地配置、Docker、示例和文档；启动器在拉起 API／Vite 前检查端口是否可绑定，并对系统保留或端口占用给出明确提示。已验证开发服务启动、API 健康检查及 Web 代理访问。

- 2026-09-08：显示剖切增加「联合剖切」与「独立剖切」。联合模式保留原有单套坐标轴／长方体和 A／B 作用范围；独立模式为模型 A、B 分别保存关闭／坐标轴／长方体、轴向范围、长方体姿态及辅助体显隐，可同时采用不同剖切方式，编辑手柄只绑定当前模型，单模型清除不影响另一模型。联合与独立参数切换后均保留，中心点、完整 Gaussian 和坐标查询复用对应模型的当前剖切状态；真实 PCD／PCD 与 PLY／PCD Gaussian 页面验证通过，浏览器控制台无错误，类型检查及 Web 构建通过。

- 2026-09-08：通用工作台工具栏增加「原点平面」。模型 A／B 可分别显示经过自身原点的 XOY、XOZ、YOZ 半透明平面，并可对每个平面独立选择不剖切、保留正轴侧或保留负轴侧；原点剖切在各模型自身坐标系判断，只裁剪对应模型，与已有世界轴／长方体剖切取交集。中心点预览、完整 Gaussian 和坐标取点可见性均已接入；平面跟随业务场景矩阵、粗配准和模型显隐。真实 PCD／PCD 与 PLY／PCD Gaussian 页面验证通过，控制台无错误，类型检查、坐标数学、配准显示及 Web 构建通过。

- 2026-09-08：ICP 成功后，视口过程状态条保留并切换为绿色完成态，显示「本次匹配已完成」及最终 RMS、点数、耗时；新一轮任务开始时自动清除完成态。状态条增加无障碍实时状态语义，浏览器实机检查确认样式生效且内容无溢出。

- 2026-09-08：修正紧凑 XYZ 输入框有值后显示不全的问题。行标题列收窄至 56 px、组内间距收窄至 6 px，数值采用 11 px 等宽字体并将轴后缀压缩到独立的 18 px 区域；370 px 侧栏实测最窄数值区为 48 px，`-91.879` 文本宽 42.33 px，可完整显示，浏览器控制台无错误。

- 2026-09-08：项目内成组 XYZ 数值输入统一为框内右侧轴名：通用与旧版粗配准采用左侧行名加右侧三输入框，上传预设、业务场景矩阵及坐标查询同步使用相同样式；补齐各输入框可访问名称。通用工作台实机检查确认粗配准两行和坐标查询六项均为 36 px 高度、无页面横向溢出，Web 构建、坐标换算与配准显示测试通过。

- 2026-09-08：业务场景矩阵参数改为单行紧凑布局，平移、旋转、缩放右侧各排列三个输入框，X／Y／Z 作为框内右侧后缀；移除与后缀冲突的数字微调箭头并补齐可访问名称。真实页面区域高度由约 752 px 降至 644 px，294 px 内容宽度无溢出，输入框保持 36 px，构建及浏览器控制台检查通过。

- 2026-09-08：模型加载后的预设编辑区统一命名为「业务场景矩阵」，保留 A／B 平移、旋转、缩放及只读矩阵并支持继续应用；压缩说明、参数行、模型分隔和矩阵框间距，输入与按钮仍保持 36 px 可操作高度。真实页面展开、保存提示及控制台检查通过。

- 2026-09-08：修正业务坐标预变换为所见即所得链路。初始预览分别显示 `P_a × p_file_a`、`P_b × p_file_b`，共同显示锚点和相机保持稳定；修改 A 的预设只改变 A，修改 B 的预设只改变 B。刚体父层保存业务局部粗配准，模型子层保存各自预变换的线性部分。
- 新工作台以 `coordinate_space=business` 提交任务。Worker 先将 A／B 点云及双精度原点分别转换到业务坐标，再执行刚体 ICP；最终 `a_to_b`／`b_to_a` 直接来自业务 ICP，文件矩阵按 `inverse(P_b) × M_business_a_to_b × P_a` 反算。旧 API 保留文件坐标配准兼容路径。
- 坐标查询以双精度业务矩阵换算，取点和显示通过各自预变换逆变换还原文件局部点；原始／配准位置切换不改变坐标对。历史恢复仅接受 `coordinate_space=business` 且预设完全一致的结果。矩阵下载、历史档案及 Worker 预编译清单已同步。

- 2026-09-07：保存业务坐标预变换中断代码快照至 `codex/business-coordinates-snapshot`（`f39abe2`），在 `codex/business-coordinates` 续接。修复业务结果组合误用于 v1 会话的问题，失败回归用例修复后通过；上传及工作台空白九参数按 `0／0／1` 处理，并拒绝浏览器判定为非法的数字输入。

- 顶部工具栏固定单排、组间竖线分隔，窄视口横向滚动；显隐文案统一「显示／隐藏」，入口及面板统一「坐标查询」。

- 视口顶部工具按配准与视图／模型显隐／高斯显示／坐标工具分组，组间竖线分隔；坐标查询改为 A／B 双列和单排点工具，常见桌面高度无需滚动。导航立方体增加轴色区分、细边框和小球角点，保留点击与拖拽。

- 配准工作台统一字号、控件高度、间距、边框与状态样式：覆盖左侧流程、视口工具栏、剖切、坐标查询及矩阵结果；暗色滚动条与页面一致，不改变 ICP 或三维交互逻辑。

- 查询编辑方只保留 A／B 点切换按钮，按钮组增加 12 px 纵向间距；查询时 ICP 执行／参数／粗调／过程勾选禁用并灰显，剖切保持可用，无点时清除及复制禁用。

- 坐标查询增加「移动 A 点／移动 B 点」按钮和平移面扩大；查询原始／配准状态均可编辑剖切，剖切与点手柄互斥并自动恢复。ICP 进度及剖切面板按工具栏实际高度定位，避免换行遮挡。

- 通用工作台已增加 ICP 坐标对应查询：A／B 原始坐标双向输入、轻量预览取点、独立平移手柄、关联红／蓝标记和坐标对复制；原始／配准显示切换不改变坐标对，退出后恢复模型姿态。粗调和角色改变后旧查询失效。
- 已提供 A／B 文件零点及 XYZ 轴独立显隐，随模型显示变换；浏览器画布尺寸随视口同步，拾取使用 PlayCanvas CSS 像素坐标。

- 已分析 Gaussian PLY 文件头、属性、点数和坐标范围。
- 已分析 GlobalMap PCD、局部扫描、位姿和地图元数据。
- 已确认 CloudCompare 手工成功流程为“PCD 作为 data、PLY 作为 model”。
- 已确认最终业务矩阵为 `T_ply_to_pcd = inverse(T_pcd_to_ply)`。
- 已核对本地 CloudCompare ICP 调用链、参数和混合精度。
- 已完成拟使用模块的初步许可证审计。
- 已确定 Windows VS2022＋Docker Desktop＋Linux Docker 的开发部署路线。
- 已固化完整实施方案文档。
- 已保存用户提供的 CloudCompare 成功 `PCD→PLY` 矩阵和计算得到的逆矩阵，作为首个真实数据黄金基线。
- 已初始化 Git `main` 分支。
- 已建立 CMake 与 Visual Studio 2022 Debug／Release Presets。
- 已实现无界面 Gaussian PLY `x/y/z` 解析器。
- 已实现 PCD v0.7 ASCII／binary `x/y/z` 解析器。
- 已实现 `4×4` 矩阵读取、通用求逆、乘法和方向回归测试。
- 已通过真实数据解析测试：PLY `3,777,901` 点，PCD `149,317` 点，均无非法坐标。
- 已通过 VS2022 Debug 构建和 CTest。
- 已从固定 CloudCompare commit 提取项目内 CCCoreLib 源码，不依赖外部 CloudCompare 安装。
- 已关闭 Qt Concurrent、TBB、CGAL 和动态库构建，Windows 静态编译成功。
- 已实现固定方向的 CloudCompare ICP 适配层：PCD 为 data、PLY 为 model。
- 已为 CloudCompare 随机采样增加可配置固定种子，种子 `42` 重复运行矩阵逐元素一致。
- 已实现 `registration_worker register`，可输出两个方向矩阵和 JSON 指标。
- 已完成真实数据配准：固定种子结果 RMS `0.222231`，最终点数 `50,000`。
- 固定种子结果与手工黄金矩阵相比，平移差约 `1.55 cm`、旋转差约 `0.17°`，处于实测随机采样波动范围内。
- 已加入真实数据 ICP 回归和确定性测试，VS2022 Release CTest `1／1` 通过，耗时约 `44.3 s`。
- 已补项目许可证正文、NOTICE、第三方声明和源码审计文档。
- 已完成 CLI 参数、错误码、运行日志、双向矩阵文件和完整 JSON 输出。
- 已完成 Ubuntu 24.04 多阶段 Docker 镜像和 Docker Compose 本地挂载配置。
- 已确认 Docker Desktop 使用 WSL2 Linux 后端，容器内 PCD 解析结果与 Windows 一致。
- 已完成容器内真实数据 ICP：Linux 与 Windows 固定种子矩阵逐元素最大差值为 `0`，RMS 均为 `0.222230612453`。
- 已在 README 补充 Docker Desktop 构建、检查、真实配准和结果目录说明。
- 已修复 GCC 识别出的 PLY／PCD 字段名临时对象引用告警，并重新通过 Windows CTest 和 Linux 编译。
- 已实现 FastAPI 流式上传、异步任务状态、结果 JSON 和结果文件下载接口。
- 已实现 C++ Worker 独立子进程调用、并发限制、超时处理和失败隔离。
- 已提供浏览器上传与矩阵结果页面，以及 Python 和 Java 11＋调用示例。
- 已完成 API 失败隔离验证：无效 PLY 返回 Worker 错误，API 进程继续健康运行。
- 已完成真实文件 API 端到端验证：上传约 257 MB PLY 和 4.8 MB PCD 后成功配准，结果矩阵与容器 CLI 基线逐元素一致。
- 已将网页和 API 的 `T_ply_to_pcd` 标记为推荐的最终业务矩阵，其余矩阵作为辅助结果展示。
- 已实现任务完成后立即清理上传文件、默认保留结果 7 天并按小时清理过期任务；清理范围仅限 `runtime/jobs/{UUID}`。
- 已提供标准 API 文档，覆盖接口、模型、错误码、状态机、矩阵约定、保留策略以及前端／Java／Python 调用方式。
- 已在上传页增加推荐／实验／自定义模式、高级参数、恢复推荐值和参数风险提示。
- 已统一页面、HTTP API 和 API 文档的默认值与参数范围，并在结果页显示实际参数、米／厘米 RMS 及误差含义警告。
- 根据 CloudCompare 手工复核结果，将“推荐模式”默认参数恢复为实测更准确的 `1e-5／50000／1.0／42`；`150000／0.95` 降级为扩大采样实验模式。
- 已增加 pnpm 统一入口，`pnpm install` 自动配置项目内 uv、Python 3.12、`.venv`、锁定依赖和可用 Worker。
- 已提供 Windows x64 静态预编译 Worker、源码指纹和二进制 SHA-256；普通用户无需 Visual Studio、CMake、Python 或 VC++ Runtime。
- 已实现 VS2022 检测、C++ 源码变化检测和 `pnpm run build:native` 自动编译、替换预编译 Worker及更新清单。
- 已提供标准 `pnpm run dev／start／test／docker:*` 命令和本地运行文档。
- 已将本地与 Docker 默认服务端口统一调整为 `8865`；本地端口由 `config/local.json` 管理。
- 已为本地与 Docker 的 Uvicorn 应用日志和 HTTP 访问日志统一增加秒级时间戳。
- 已实现自适应体素预览：真实 `3777901` 点 PLY 生成 `299863` 点中心预览和同源轻量 Gaussian PLY，PCD `149317` 点完整保留。
- 已实现人工粗配准会话 API，原始文件只上传一次并复用于预览和最终 ICP。
- 已实现 `T_pcd_to_ply = T_icp_delta × T_manual_pcd_to_ply` 初始矩阵链路及刚体矩阵双重校验。
- 已实现 PlayCanvas 工作台：PLY 固定、PCD 平移／旋转 Gizmo和数值输入、矩阵实时显示、中心点／Gaussian 按需切换；平移与旋转 Gizmo 采用互斥挂载，顶部模式、手柄和数值输入状态保持一致。
- 已实现视口独立导航：中键或 `Shift＋左键` 平移、右键围绕模型旋转、滚轮缩放；普通左键仅用于 PCD Gizmo，不再控制相机。
- 已增加视口右上角实体导航立方体：六个面对应 `±X／±Y／±Z` 轴向视图，八个屏幕朝向的球形拐角对应等轴视角，左键拖拽立方体可连续旋转主视口；主视口和立方体采用基于视口尺寸的一致角度换算。
- 视口平移已按透视 FOV／相机距离或正交高度精确换算每像素世界距离，替代经验速度系数，避免鼠标锚点与场景移动不同步。
- 相机轨道已由受限的 yaw／pitch 欧拉角改为四元数旋转，移除 `±89°` 俯仰限制和极点方向翻转，可连续跨越顶部、底部及累计旋转超过 `360°`。
- 主视口与导航立方体增加旋转意图锁定：起始拖拽明显为垂直或水平时锁定对应轨道，抑制跨越极点时微小横向／纵向抖动造成的偏航；斜向起步仍使用自由四元数旋转。
- 导航立方体姿态改为直接使用相机方向／上方向／右方向组成的正交基 `matrix3d`，不再将四元数姿态反算为会在 90° 极点跳变的 yaw／pitch；同时取消 transform 过渡动画。
- 四元数拖拽符号已按实际屏幕坐标校准：水平绕相机上方向使用负轴角，垂直绕相机右方向使用负轴角，使主视口与导航立方体的左右、上下拖拽均与画面方向一致。
- 已将平移与旋转 Gizmo 的输入严格限制为左键，关闭 PlayCanvas 默认开启的中键／右键变换；视口空白区域使用左键旋转，仅中键平移，右键不绑定任何功能。
- 已将 Vite＋TypeScript 前端纳入 pnpm 本地开发、生产静态服务和 Docker 多阶段构建。
- 已将上传、预览、人工粗配准、ICP 参数、任务进度和最终矩阵合并为单页工作台；上传后自动加载三维预览，PCD 粗配准工具浮动在视口内，可跳过粗配准直接执行 ICP。
- 已完成统一工作台真实数据端到端验证：`50000` 点 ICP 成功，RMS `0.222231 m`，结果 `PLY→PCD` 矩阵可见，最终 `PCD→PLY` 对齐效果已回显到三维视口。
- Gaussian 视觉确认已改为按需流式加载原始 `256898154` 字节、`3777901` 点 PLY；轻量中心点仍为默认模式，切回后卸载原始 Gaussian 并释放 GPU 资源，预览阶段不再生成抽样 Gaussian PLY。
- 已实现两阶段高采样稳定性模式：先执行 `50000` 点 CloudCompare 基线，再以 `500000` 点上限运行种子 `42／43／44`；真实数据最终使用完整 `149317` 点 PCD，RMS `0.157874 m`，总耗时 `178.57 s`，平移重复性 `0.00370 m`、旋转重复性 `0.05433°`，通过当前重复性阈值。
- 高采样真实矩阵与手工 CloudCompare 黄金矩阵仍存在约 `0.0328 m／0.3406°` 差异，因此默认业务模式继续使用已人工验证的 `50000` 点推荐基线；高采样结果仅证明重复性，不能替代控制点或实飞精度验证。
- 本地 Python 全部传递依赖已由 `uv.lock` 锁定；Docker 改用带哈希的完整 requirements 锁文件。
- 已将推荐模式准确标注为 CloudCompare 默认参数基线；模式切换会同步展示并提交实际采用的第一、第二阶段参数，高采样模式明确区分统计重复性与绝对精度。
- 最终结果区已增加一键复制 `PLY→PCD` 业务矩阵按钮，并提供复制成功或失败的显式反馈。
- 人工配准工作台已改为多轮状态机：仅在 ICP 运行期间锁定 PCD 和参数，成功或失败后恢复粗调、模式切换及参数编辑；每轮始终以当前视口绝对 `PCD→PLY` 矩阵作用于原始点云。
- 人工会话已持久化各轮任务状态、实际参数、初始矩阵和结果地址，刷新页面可恢复历史；同一会话的并发提交由服务端返回 `409` 阻止。
- 结果完成后再次移动 PCD 或修改参数会显式标记上一次业务矩阵过期；每轮历史均可查看、复制，并可连同该轮参数恢复到视口。
- 已引入 LASzip 官方 `3.5.0` 源码并静态编译，C++ Worker、HTTP API 和 PlayCanvas 工作台支持 PCD／LAS／LAZ 定位参考点云。
- LAS／LAZ 使用双精度 scale／offset 解码和包围盒中心局部化；最终 `T_ply_to_reference` 在双精度中恢复世界原点，浏览器视口与业务矩阵分别使用局部和世界矩阵。
- 已保留 PCD multipart 字段和结果字段兼容性，同时增加 `reference_format`、`reference_origin`、`reference_local_to_ply`、`reference_to_ply` 与 `ply_to_reference` 通用字段。
- 已更新 Windows x64 预编译 Worker；无 Visual Studio 用户通过 `pnpm install` 可直接获得 LAS／LAZ 支持。
- 已完成通用双模型配准升级：模型 A、模型 B 均支持 PLY／PCD／LAS／LAZ，业务输出方向与 ICP 移动角色相互独立，自动角色只作建议、不限制用户选择。
- C++ Worker 已增加 `register-models` 和 `prepare-model-preview`，始终输出 `T_a_to_b`、`T_b_to_a` 及方向明确的 moving-local→fixed-local 初始、增量和最终矩阵。
- 已提供 `/api/v2/registration-sessions` 通用会话接口和 PlayCanvas 双模型工作台；仅当前移动模型可粗调，结果区显式显示所选业务矩阵并提供反向矩阵。
- 已保留全部 `/api/v1` 接口和 PLY／定位参考点云旧工作台；真实 PLY／PCD 的 v2 `T_a_to_b` 与原 v1 `T_ply_to_pcd` 逐元素一致。
- 通用工作台的业务方向和移动模型选择器已统一为纵向全宽表单布局；右上角恢复实体视角立方体、六面／八拐角快速视角、拖拽旋转及正交／透视切换，并复用原工作台的无极点翻转四元数相机轨道。
- 通用工作台的反向矩阵展开区已增加独立一键复制按钮；正向与反向复制均提供成功／失败反馈，且反向内容随当前业务方向正确选择。
- 通用工作台执行 ICP 时显示旋转加载图标和“ICP 配准中”，并提供真正终止 C++ Worker 的按钮；任务取消后状态为 `cancelled`，自动解除会话占用、恢复粗配准和参数控件，可直接修改后重新提交。
- ICP 执行期间，平移、旋转和“重置粗配准”按钮均设置原生 `disabled`、阻断指针与事件并显示灰色禁用态；“重置粗配准”清除当前移动模型的平移和旋转并恢复原始加载位置；仅“适应全部”和视角控制器保持可用，因为它们只调整相机、不改变模型矩阵。
- 已实现可选的 ICP 实时过程显示：CCCoreLib 在每个已接受迭代后只读报告累计变换，Worker 输出 JSON Lines，FastAPI 通过 SSE 转发，PlayCanvas 逐轮更新当前移动模型并显示迭代、RMS、点数和耗时。
- 实时过程默认关闭，但勾选框在 ICP 运行期间保持可操作；通用任务始终生成轻量事件，中途勾选直接接入当前最新一轮，取消勾选只停止视口动画，再次开启继续接回当前进度。成功时以最终结果矩阵覆盖动画姿态，终止时保留最后已显示的中间姿态并明确标记为未收敛、不可用于航点转换。
- ICP 逐轮信息显示在三维视口工具栏下方；当移动模型包围盒对角线达到固定模型的 `1.25` 倍时，方向区显示范围风险、实际倍数和交换 ICP 角色建议，同时保持业务矩阵方向独立。
- 三维视口左上角第一排粗配准工具栏末尾已增加模型 A、B 独立显示开关；可单独或同时隐藏并恢复，隐藏移动模型时同步隐藏变换手柄，ICP 运行期间保持可操作，且不改变点云数据、粗配准矩阵或最终结果。
- 移动／固定模型及黄色／灰色说明已从三维视口移入左侧第 1 步“模型”区域，并随移动角色选择同步更新；ICP 逐轮信息上移到视口工具栏下方。
- 已将 `feat/manual-coarse-registration` 开发分支以 fast-forward 方式合并到本地 `main`；合并后 Windows Release 原生测试 `1／1` 通过，并重新生成与当前源码指纹一致的 Windows x64 预编译 Worker。
- 已将 `feat/point-cloud-clipping` 以 fast-forward 方式合并到本地 `main`；主分支现已包含 Web GPU 显示剖切：支持 X／Y／Z 最小与最大范围组合、有向长方体内保留、A／B／双模型作用范围、长方体移动／旋转／尺寸调整、适配单模型或全部模型及辅助边界显示。
- 剖切与模型粗配准 Gizmo 已互斥，关闭剖切面板可继续粗调；剖切状态不进入 ICP 请求，不修改模型矩阵、原始点云、RMS 或最终业务矩阵。
- 剖切面板已统一为工作台深色表单风格；坐标轴模式在三维场景显示 X／Y／Z 轴和六个正负向拖拽手柄，拖动期间临时显示对应半透明剖切面，松开后隐藏；手柄与面板数值双向同步，拖动手柄会自动启用对应边界。
- 长方体模式已提供六个独立面手柄，悬停时高亮并显示方向，拖动只调整对应面；中心同时挂载轴向／平面平移手柄和三轴旋转环，平移与旋转使用独立拾取层，不再发生平移手柄误触旋转。
- 剖切边界手柄已统一为方向箭头：坐标轴模式以六条完整可见正／负半轴作为屏幕命中区域，鼠标进入任意半轴附近即在对应剖切边界显示箭头，无需靠近隐藏手柄或等待；长方体模式按可见面的屏幕投影识别悬停面，短暂停留后只显示该面的自适应箭头，避免六面手柄常驻遮挡模型。
- 长方体中心组合 Gizmo 已移除中央自由旋转拾取，平移轴与平面保持优先；旋转输入仅在 PlayCanvas 实际圆环拾取命中时开启，圆环内部空白区域不再触发旋转，缩小盒体后仍可区分轴向／平面平移与圆环旋转。
- 长方体辅助边线、六个居中贴面小面＋箭头手柄和中心平移／旋转 Gizmo 已改为由「显示剖切辅助体与手柄」统一常显／隐藏；取消显示不影响长方体 GPU 剖切效果。
- 模型粗配准已改为无模式平移／旋转组合 Gizmo，移除顶部“平移 G／旋转 R”切换；平移轴／平面和旋转圆环始终同时挂载当前 ICP 移动模型，沿用平移优先、圆环窄带旋转的输入仲裁，ICP 运行期间整体隐藏。
- 视口「剖切」按钮已绑定实际剖切模式：坐标轴或长方体剖切启用时显示绿色激活态，关闭面板后仍保留该状态；切换到「关闭」后恢复默认态。
- 通用双模型工作台已支持模型 A／B 独立切换完整 Gaussian：仅包含 Gaussian 属性的 PLY 可用，按需加载原始 PLY、隐藏同模型中心点并继承粗配准／ICP 变换；切回中心点时销毁实体并卸载 GPU 资源。按钮以「显示原高斯／切回中心点／无高斯数据」明确区分操作与不可用状态。
- 完整 Gaussian 已与坐标轴／有向长方体剖切联动：复用中心点预览的世界坐标边界和 A／B 作用范围，在 PlayCanvas GSplat 工作缓冲中将边界外椭球缩放为零；拖动期间按 `75 ms` 限频重建，松手强制同步最终范围，且仍只影响视觉预览、不进入 ICP 输入。
- v2 配准成功后已生成独立轻量历史档案，持久化双向矩阵、模型摘要与 SHA-256、ICP 参数、RMS、服务版本和时间；历史按 `workspace_id` 隔离，不依赖会被释放的 Job 目录。
- v2 源模型和预览默认保留 24 小时，支持延长 24 小时、立即释放和重新进入会话；运行中任务禁止释放，源文件清理后历史矩阵仍可查看和复制，但继续配准被禁用。
- 首页已增加右侧时序历史区，按完成时间倒序显示模型、指标、最终矩阵和源文件到期状态；桌面使用左右两栏，窄屏自动改为上下布局。

## 历史进行中记录

- 补充统一配准工作台的自动化浏览器回归。
- 补充自动化 API 回归测试脚本。
- 使用部署环境提供的端点与凭证完成外部 S3／MinIO 实例联调。

## 待办

> 以下为早期阶段记录，包含已经完成的条目和旧版表述，不作为当前重构待办依据；当前未完成事项以文档顶部「进行中」为准。

### 阶段 1：项目骨架与许可证基线

- 初始化 Git。
- 创建 CMake 工程和 Visual Studio 2022 Presets。
- 创建许可证、NOTICE、第三方声明和逐文件审计表。
- 建立 Windows 与 Linux 的最小编译测试。

### 阶段 2：文件解析

- 已采用无界面流式解析实现 Gaussian PLY Reader。
- 已实现 PCD Header、ASCII 和 binary 解析。
- 已完成当前真实文件解析测试。
- 待补解析器异常格式和截断文件测试。

### 阶段 3：ICP 核心

- 已提取 CCCoreLib 源码并建立固定上游基线。
- 已封装固定方向的 PCD→PLY ICP。
- 已输出 PCD→PLY 与 PLY→PCD 矩阵。
- 已建立 CloudCompare 黄金矩阵真实数据回归测试。
- 待增加小型合成点云 ICP 测试。

### 阶段 4：C++ CLI

- 实现参数解析、配置、日志和错误码。
- 实现任务目录和标准 JSON 输出。
- 完成 Windows Release 构建与真实数据验证。

### 阶段 5：Docker Linux

- 已编写 Ubuntu 24.04 多阶段 Dockerfile。
- 已完成容器内编译、文件解析和真实数据回归。
- 已验证 Windows 与 Linux 固定种子矩阵逐元素一致。

### 阶段 6：HTTP 服务与网页

- 已实现流式上传和异步任务接口。
- 已实现 C++ Worker 子进程隔离、并发限制和超时控制。
- 已实现任务状态、矩阵显示、下载和错误信息页面。
- 已提供 Java、Python 调用示例。

### 阶段 7：自动粗配准与生产化

- 评估和接入 4PCS 自动粗配准。
- 增加多 overlap 候选与结果评分。
- 增加 MinIO／S3、任务队列、保留策略和负载测试。

### 阶段 6.5：本地运行与分发

- 已支持 Windows x64 `pnpm install` 后直接 `pnpm run dev`。
- 已支持无 VS 使用预编译 Worker，有 VS 自动构建和替换 Worker。
- 已验证本地模式和 Docker 模式共用同一 API 与网页。

## 阻塞

- 2026-09-13：Codex 内置浏览器因管理员安全策略校验不可用，拒绝访问 `http://127.0.0.1:5173`。后续已通过独立 Chrome／Playwright CLI 完成大部分浏览器、三维视觉和长期 GPU 验收；该历史限制不再阻塞当前剩余专项。
- 当前黄金矩阵只有 6 位小数，且未知 CloudCompare 随机采样种子；可以用于几何回归，但不能作为逐位一致基线。若能导出更高精度矩阵，应替换当前基线。
- 尚未确认最终公开仓库地址和真实测试数据是否随项目公开。

## 最近验证

```text
日期：2026-09-09
方式：Vue 类型检查＋Vite 生产构建＋Playwright 真实浏览器；既有 Windows CTest、服务单元测试和业务坐标回归基线沿用第一批验证结果
重构第二批：真实历史会话中验证独立剖切 A 坐标轴／B 长方体组合、编辑模型切换、Gaussian 加载／剖切同步／资源释放，浏览器控制台 0 错误、0 警告。
Pinia 草稿：浏览器设置 B→A 和移动 A 后进入真实历史工作台，再通过「新建」执行 SPA 返回，设置保持且控制台 0 错误、0 警告。
服务拆分：`python -m compileall -q service` 通过，服务端单元测试 9／9 通过。
C++ 外壳：Windows Release Worker 重建完成，CTest 1／1（27.30 秒）通过；无参数返回用法及退出码 10，缺失 `--ply` 参数返回原错误文案及退出码 50。
剖切场景：真实会话启用联合坐标轴剖切与 X 最小边界后加载 A Gaussian，状态显示与当前剖切同步；释放资源后恢复中心点，控制台 0 错误、0 警告。
坐标查询：真实历史结果输入 A `(3,4,5)` 后得到 B `(1.527641002114,-3.851035931648,3.894179693202)`；编辑点和显示状态切换不改变坐标对，关闭查询并离开工作台后控制台 0 错误、0 警告。
存储拆分：`python -m compileall -q service` 通过，服务端单元测试 9／9 通过，测试临时 `RUNTIME_ROOT` 注入保持有效。
Web 路由：`/health`、`/`、真实 `/registration/{session_id}` 返回 200，随机不存在会话返回 404；服务端单元测试 9／9 通过。
结果：Windows CTest 1／1、服务端 9／9、坐标数学及显示回归通过。已知对应关系合成点云在 A 非等比缩放及复合旋转下，移动 A／B 两轮业务 ICP RMS 分别为 7.41627e-7 m、5.04159e-7 m；业务矩阵接近单位矩阵，反算文件矩阵接近 `P_a`，正反向、四种下载和历史档案一致。
真实数据：3777901 点 PLY／149317 点 PCD 在 `A Rx=-90°`、B 单位预设下完成业务 ICP，RMS 为 0.457745013019 m。浏览器输入 A 业务坐标 `(3,4,5)` 得到 B 业务坐标 `(3.031071691123,8.727348067593,-1.561734026339)`；原始／配准显示切换不改变坐标值，控制台无错误。
所见即所得：同一真实会话和相机下，A 的 `Rx=0°` 与 `Rx=-90°` 初始截图明显不同；显示回归进一步断言修改 A 不改变 B。证据为 `output/playwright/business-initial-a-rx0.png`、`output/playwright/business-initial-a-rx-90.png` 及 `runtime/business-verification/{synthetic,real}-summary.json`。
构建：原生 Worker 已重建并更新 Windows x64 清单；Vite 生产构建和 `git diff --check` 通过，保留现有 PlayCanvas worker_threads 外置及大包提示。Docker 未改动、未重建。
复现：`pnpm run test`；`pnpm run test:service`；`pnpm exec node --experimental-strip-types web/src/coordinate-math.test.ts`；`pnpm exec node --experimental-strip-types --experimental-transform-types web/src/registration-display.test.ts`；启动独立服务后运行 `.venv/Scripts/python.exe tests/service/business_transform_e2e.py http://127.0.0.1:8766`，可加 `--real`。
```

- 按最新要求将立方体文字恢复为面内标签：撤去屏幕朝向补偿及向外抬升，仅在文字倒置时面内旋转 180°，保留透视与镜像修正。浏览器斜视截图及标签面内变换断言通过，类型检查和 Web 构建通过；此项替代此前的屏幕朝向标签方案。

- 立方体文字显示补充修复：面标签独立抵消面与立方体的旋转，始终面向屏幕；轻微前移避免被倾斜面遮挡，文字不参与鼠标命中。浏览器已检查六个轴向的标签组合矩阵及截图，−Y／−Z 顺序正常；斜视角截图文字正向完整。TypeScript、Web 构建和立方体数学回归通过。

- 视角立方体镜像修复：通用／旧版页面采用双侧 CSS Y 轴转换及正交化上方向，修正 Y／−Y 面位置；构建、类型检查及 `node web/src/view-cube.test.mjs` 通过，六个轴向和斜视角的旋转行列式均为 +1，角点逆矩阵正确。浏览器视觉复测待完成。

- 高斯按钮文案修正为下一步动作：初始／释放高斯／加载失败回退显示「A／B：高斯」，加载成功显示「A／B：点云」；重置按钮简写为「重置」。类型检查、Web 构建及差异检查通过；本次浏览器复测因本地 5173 服务未运行（连接被拒绝）未完成，实际渲染切换待验证。

- 2026-09-06：单排工具栏及文案通过类型检查、Web 构建；浏览器 1366×768 检查全部工具按钮顶边一致，模型隐藏／显示往返及高斯、坐标查询入口文案断言通过。

- 2026-09-06：工具栏分组及查询面板紧凑布局通过 TypeScript、Web 构建和坐标数学测试；真实 ICP 后浏览器检查 1366×768、1366×650 视口，查询面板无纵向溢出；立方体面点击、拖拽改变视角、正交按钮激活检查通过。更低视口保留滚动兜底。

- 2026-09-06：统一工作台样式后，`pnpm run build:web`、TypeScript 检查、坐标数学测试与 `git diff --check` 通过。浏览器以 1366×900、1800×1000 检查真实双模型任务、ICP 完成后的查询／剖切面板；抽查左侧执行与视口／剖切按钮均为 13 px、36 px 高度，查询时 ICP 保持禁用。构建保留 PlayCanvas 浏览器外置模块和大包体积提示。

```text
日期：2026-09-06
方式：真实 ICP 后浏览器按钮状态检查＋TypeScript／Vite
结果：重复下拉框已移除，查询时 ICP 等控件禁用且剖切可用，无点／有点复制状态切换正确，退出后 ICP 恢复；按钮组 CSS 间距 12 px，类型检查与构建通过。
```

```text
日期：2026-09-06
方式：1366×900 浏览器真实 ICP＋查询与剖切交互＋TypeScript／Vite＋坐标数学测试
结果：页面首次执行 ICP 后显示实时轮次，进度框位于换行工具栏下方；移动 B 点切换、查询时长方体剖切、关闭后恢复取点、原始位置轴向剖切通过。类型检查、生产构建与坐标数学测试通过。
```

```text
日期：2026-09-05
方式：真实 PLY／PCD ICP＋Playwright 坐标查询交互＋双精度数学测试＋TypeScript／Vite
结果：拾取、数值编辑、B→A 反算与平移手柄通过；切换原始位置后坐标对不变，退出后 ICP 初始矩阵恢复一致，重置粗配准禁用旧查询；A／B 原点轴可独立显示。旋转／逆变换／大坐标原点还原测试通过，类型检查与生产构建通过。第一版查询点仅在当前页面保存，未进行真实 LAS／LAZ 浏览器取点验证。
```

```text
日期：2026-09-03
方式：Python 生命周期单元测试＋Python 编译＋Vite 生产构建＋Playwright 桌面／窄屏浏览器检查＋Docker Compose 配置解析＋Windows CTest
结果：3 个历史生命周期测试通过，覆盖源文件释放后矩阵存续、工作区隔离和运行中释放保护；首页双栏／响应式布局与空历史状态正常，浏览器控制台 0 error；Docker Compose 配置有效；Windows registration_tests 1／1 通过
```

```text
日期：2026-09-03
方式：真实双模型会话 Playwright 坐标轴独立命中及 Z 边界重复调整回归＋TypeScript／Vite
结果：半轴命中改为由原点附近投影方向生成的全视口二维射线，不再使用可能越过相机的三维远端点；鼠标位于 Z 轴与密集点云重叠处、距原点约 13 px 时可立即显示 +Z 手柄；将 Z 最大值从 80.842 m 拖至约 197.03 m，移出后再次回到同一重叠位置仍能重新显示已移动手柄；+Y 半轴中段同样正常，浏览器控制台 0 error；TypeScript 类型检查和 Vite 生产构建通过
```

```text
日期：2026-09-03
方式：真实双 Gaussian PLY 会话 Playwright 轴向／长方体剖切回归＋完整 pnpm build
结果：117150784 字节、1722792 点的模型 A 完整 Gaussian 加载后，X 最小边界从关闭切换为 0.000 m 可见地裁掉边界外高斯椭球；作用范围切为“仅模型 B”后模型 A 完整恢复，证明 A／B 裁剪状态相互独立；长方体模式及适配 B 正常更新，浏览器控制台 0 error；C++ Windows Release 构建、Vite 生产构建及 registration_tests 1／1 通过
```

```text
日期：2026-09-03
方式：真实双 Gaussian PLY 会话 Playwright 回归＋PCD 双模型兼容回归＋TypeScript／Vite
结果：117150784 字节、1722792 点的模型 A 成功按需加载完整 Gaussian；可用按钮以蓝紫色显示「显示原高斯」，激活后以绿色显示「切回中心点」，模型显示开关及剖切边界提示同步；切回后恢复中心点并卸载 Gaussian；PCD／PCD 会话明确显示「无高斯数据」并禁用按钮；浏览器控制台 0 error；TypeScript 类型检查和 Vite 生产构建通过
```

```text
日期：2026-09-02
方式：真实 PCD 双模型 Playwright 长方体辅助体统一显示开关、模型粗配准组合 Gizmo 回归＋TypeScript／Vite
结果：坐标轴正负半轴悬停约 350 ms 后才显示边界箭头，拖动 +X 箭头将 X 最大值从 `6.123` 更新为 `7.113` 并自动启用该边界；长方体的完整边线、六个居中贴面小面＋外向箭头及中心平移／旋转 Gizmo 在「显示剖切辅助体与手柄」勾选时持续显示，取消勾选后三者均隐藏而点云剖切预览继续生效；模型粗配准顶部不再显示平移／旋转模式按钮，平移轴拖动使 X 从 `0.000` 变为 `1.310 m`，旋转圆环拖动同步更新三轴角度，重置后六个数值恢复 `0.000`；浏览器控制台 0 error；TypeScript 类型检查和 Vite 生产构建通过
```

```text
日期：2026-09-01
方式：真实 PCD 双模型 Playwright 鼠标拾取与拖拽回归＋TypeScript／Vite／CTest
结果：坐标轴 +Z 手柄拖动期间显示蓝色半透明剖切面，面板值同步更新并自动启用 Z 最大边界，松开后剖切面立即消失；长方体轴向与平面平移后盒体位置改变且未触发旋转，旋转环仍可独立旋转，分离拾取层生效；浏览器控制台 0 error；TypeScript 类型检查、Vite 生产构建和 Windows registration_tests 1／1 通过
```

```text
日期：2026-09-01
方式：真实约 30 万点双模型预览的 Playwright 剖切手柄回归＋TypeScript／Vite／CTest
结果：坐标轴模式显示三色世界轴和六个正负向手柄；拖动 +X 后面板最大值由 179.928 m 同步为 224.396 m 并自动启用边界，面板修改也实时回写场景；长方体六面手柄悬停高亮并显示方向，拖动 +Y 仅改变对应面；中心平移手柄和旋转环同时可见且旋转生效；浏览器控制台 0 error；TypeScript 类型检查、Vite 生产构建和 Windows registration_tests 1／1 通过
```

```text
日期：2026-09-01
方式：真实约 30 万点双模型预览的 Playwright GPU 剖切回归＋旧版工作台兼容检查＋TypeScript／Vite／CTest
结果：坐标轴 X 最小值剖切实时生效；有向长方体适配 B 后只保留盒内点，切换为仅作用 B 时 A 完整恢复；移动／旋转／尺寸 Gizmo、辅助边界和模式提示正常，当前测试过程浏览器控制台 0 error；旧版 PLY／LAS 工作台正常加载；TypeScript 类型检查、Vite 生产构建和 Windows registration_tests 1／1 通过
```

```text
日期：2026-09-01
方式：开发分支合并 main 后运行 pnpm run test
结果：Visual Studio 2022 Release 构建成功，registration_tests 1／1 通过；Windows x64 预编译 Worker及其源码指纹、SHA-256 清单已按合并后的源码重新生成
```

```text
日期：2026-09-01
方式：Playwright 模型角色说明布局回归
结果：左侧第 1 步“模型”区域显示“移动 A（黄色）／固定 B（灰色）”，三维视口不再显示该说明排；角色选择变化仍由同一 refreshRoles 链路同步；模型 A／B 显示按钮保留在第一排工具栏末尾，浏览器控制台 0 error；TypeScript 类型检查和 Vite 生产构建通过
```

```text
日期：2026-09-01
方式：Playwright 模型 A／B 独立显示、双模型同时隐藏、恢复及 ICP 运行中切换回归
结果：A、B 均可独立切换为“已隐藏”并恢复；隐藏当前移动模型时手柄同步卸载；ICP 运行中平移、旋转和重置保持禁用，但 A／B 显示按钮仍可操作，终止正常，浏览器控制台 0 error；TypeScript 类型检查和 Vite 生产构建通过
```

```text
日期：2026-09-01
方式：在用户当前 8765 端口的旧版后端进程上加载新版前端，未勾选启动 ICP 后再中途开启过程显示
结果：确认原故障由前后端热更新版本不一致引起，旧后端仍依赖 show_registration_progress，而新版前端此前未传该字段；兼容修复后前端固定传 true，旧后端任务中途开启时成功显示第 91 轮 RMS 和模型姿态，取消正常，浏览器控制台 0 error；新版后端仍保持无条件生成事件和 from_latest 直接接入最新轮次
```

```text
日期：2026-09-01
方式：真实 149317 点 LAS／3777901 点 PLY 未开启显示启动 ICP，再于运行中开启、关闭和重新开启过程显示
结果：运行中模型、方向和 ICP 参数保持禁用，过程显示勾选框独立可用；中途首次开启直接显示当前第 76 轮而非从头回放，关闭不影响 Worker，重新开启后接入后续进度，任务最终成功并以第 193 轮最终矩阵覆盖视口；浏览器控制台 0 error，Python 编译、TypeScript 类型检查和 Vite 生产构建通过；Docker Desktop Linux 29.2.0 镜像重建成功，容器 OpenAPI 已包含 `from_latest` 查询参数
```

```text
日期：2026-09-01
方式：真实 149317 点 LAS／3777901 点 PLY 的 Playwright 角色切换、范围风险和实时视口布局回归
结果：小范围 A 移动时不报警；切换为大范围 B 移动时显示包围盒对角线约 3.0 倍及交换角色建议；开启过程显示后第 16 轮状态位于视口工具栏、移动／固定标识之后的第三排；取消恢复正常，浏览器控制台 0 error；TypeScript 类型检查和 Vite 生产构建通过
```

```text
日期：2026-09-01
方式：真实 149317 点 LAS／3777901 点 PLY 的 C++ JSON Lines＋FastAPI SSE＋Playwright 逐轮视口与取消回归＋Windows／Docker Linux 构建
结果：真实任务连续收到逐轮事件，浏览器第 16 至 43 轮的 RMS、点数、耗时和移动模型姿态持续更新；运行期间模型与参数控件全部禁用，取消后恢复并显示未收敛警告，控制台 0 error；最后迭代矩阵与最终 moving-local→fixed-local 完全一致，开启／关闭进度时最终矩阵最大元素差和 RMS 差均为 0；Windows CTest 1／1 通过，TypeScript、Python 编译和 Vite 构建通过，Ubuntu 24.04 Docker 镜像重建成功并包含 --progress-jsonl
```

```text
日期：2026-09-01
方式：真实 LAS／3777901 点 PLY 运行中取消＋同会话重新提交＋Playwright 按钮状态回归
结果：运行中 Worker 被终止，任务状态为 cancelled／error_code=task_cancelled，会话 active_job_id 清空，registration_worker 残留进程数为 0；同一会话重新提交成功；页面执行中显示加载动画和终止按钮，取消后所有方向、粗配准和 ICP 参数控件恢复，提示可重新执行，浏览器控制台 0 error
```

```text
日期：2026-09-01
方式：通用工作台 TypeScript／Vite 构建＋Playwright 视觉与交互回归
结果：方向与移动模型标签、选择器纵向对齐且宽度一致；右上角三维立方体和球形拐角显示正常；可见 Y 面点击切换视角，正交按钮成功激活；浏览器控制台 0 error
```

```text
日期：2026-09-01
方式：通用双向合成测试＋真实 PLY／PCD 回归＋LAS／LAZ v2 API＋Playwright 浏览器端到端＋Windows 预编译分发
结果：移动 A 和移动 B 均恢复同一预期 A→B 世界矩阵，正逆往返通过；真实数据 v2 与 v1 业务矩阵最大元素差为 0；LAS→LAZ 指定移动 B 成功返回双向单位矩阵和双精度原点；浏览器完成上传、移动角色 B、预览、ICP、T_a_to_b 显示和反向矩阵展示，控制台 0 error；Windows CTest 1／1 通过，前端类型检查和生产构建通过；Ubuntu 24.04 Docker 镜像重建成功，容器内移动 B／输出 B→A 配准成功，容器 API 健康检查及两个 v2 OpenAPI 路径通过
```

```text
日期：2026-09-01
方式：LASzip 合成 LAS／LAZ 大坐标单元测试＋真实 Gaussian PLY／LAZ API 端到端＋Windows／Docker Linux 构建
结果：等价 LAS 与 LAZ 的点数、世界包围盒、局部原点和逐点局部坐标一致；630499／4834749 量级坐标局部化恢复误差小于 1e-7 m，世界矩阵正逆往返通过；现有真实 PLY／PCD CloudCompare 回归继续通过；257 MB PLY＋LAZ 会话预览和 ICP 成功并返回世界业务矩阵；Windows pnpm 测试 1／1 通过；Ubuntu 24.04 Docker 静态构建成功，容器内 LAZ 解码成功
```

```text
日期：2026-08-21
方式：真实 PLY／PCD 两轮 ICP＋Playwright 多轮工作台回归＋会话刷新恢复＋Python／TypeScript／Vite 验证
结果：第 1 轮从单位矩阵得到 RMS 0.222231 m；完成后所有粗调和参数控件恢复。将当前 PCD 绝对 X 平移改为 0.5 m 后旧矩阵立即标记过期，第 2 轮服务端记录 initial_tx=0.5 并得到 RMS 0.222236 m；页面同时保留两轮历史，刷新后恢复两轮，恢复第 1 轮可同步恢复视口矩阵和参数。Python 编译、TypeScript 类型检查、Vite 生产构建和 git diff 检查通过，浏览器控制台 0 error
```

```text
日期：2026-08-20
方式：TypeScript 类型检查＋Vite 生产构建＋Playwright 模式切换与剪贴板交互回归
结果：推荐模式显示并恢复 CloudCompare 默认采样上限 50000；高采样模式显示第一阶段 50000、第二阶段上限 500000、当前 PCD 149317 点、3 次运行、种子 42／43／44 和稳定性阈值；复制按钮返回“已复制 PLY→PCD 矩阵”；浏览器控制台 0 error
```

```text
日期：2026-08-20
方式：pnpm 首次安装／重复安装＋托管 Python＋预编译 Worker＋VS2022 原生构建＋本地 FastAPI＋CTest＋Docker 哈希锁定构建
结果：首次 pnpm install 自动安装 uv 0.12.3、Python 3.12.13 和 14 个运行包；重复安装约 0.9 s；强制无 VS 模式成功使用 SHA-256 校验的预编译 Worker；pnpm run build:native 成功替换静态 Worker且二进制仅依赖 KERNEL32.dll；pnpm run dev 健康页返回 200，失败任务隔离和输入清理通过；pnpm run test 1／1 通过，约 39.5 s；Docker 使用完整哈希锁文件构建成功
```

```text
日期：2026-08-20
方式：真实大文件预览＋PlayCanvas 浏览器检查＋人工矩阵 API 端到端＋Windows CTest＋本地生产静态服务＋Docker 多阶段构建
结果：PLY 预览 299863 点／3.6 MB，轻量 Gaussian PLY 约 20.4 MB，PCD 预览 149317 点；浏览器中心点、Gaussian、Gizmo 和数值矩阵同步通过；单位初始矩阵 ICP RMS 0.222230612453，与原基线一致；CTest 1／1 通过，约 47.6 s；Docker 健康页和主页均返回 200
```

```text
日期：2026-08-20
方式：Vite 生产构建＋Playwright 真实浏览器视口导航回归
结果：中键与 Shift＋左键平移使用投影参数计算每像素世界位移，普通左键不再平移相机；导航立方体采用独立的 0.6°／px 拖拽灵敏度，主视口右键旋转保持视口归一化角速度；立方体仅在超过 5 px 后进入拖拽并捕获指针，普通单击可稳定触发可见面和球形拐角视角切换；浏览器控制台 0 error
```

```text
日期：2026-08-20
方式：TypeScript 类型检查＋Vite 生产构建＋Playwright 四元数轨道连续旋转回归
结果：连续两次纵向旋转约 266°，成功跨越相机顶部和底部，无角度卡止；从 +X 视图纯向上拖动并注入约 ±3 px 横向抖动，跨越 90° 后仍保持垂直轨道；导航立方体正交基矩阵连续更新，无欧拉角 180° 跳变和 CSS 大幅补间；控制台 0 error，PCD 的 T_manual_pcd_to_ply 始终保持单位矩阵
```

```text
日期：2026-08-20
方式：TypeScript 类型检查＋Vite 生产构建
结果：根据浏览器实测单独反转水平四元数轴角，垂直方向保持不变；类型检查与生产构建通过
```
