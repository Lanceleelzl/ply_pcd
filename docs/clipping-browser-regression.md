# 剖切浏览器回归

## 2026-09-11：画布适配修复后的联合六向实拖

1920×1080，真实会话恢复后，按当前标签投影位置定位并确认悬停，再执行拖动；每次先重置范围或重新适配长方体。

- 联合长方体六面 −X／+X／−Y／+Y／−Z／+Z 均命中并移动，屏幕位移依次约为 16.20／14.93／20.19／20.72／16.81／16.13 像素。每次对侧面投影位移为 0，模型初始矩阵及导航立方体姿态不变。
- 联合轴向 −X 从 −32.265 变为 −29.924，+X 从 12.689 变为 11.354；−Y 从 −12.742 变为 −10.954，+Y 从 44.618 变为 40.267；−Z 从 −6.647 变为 −5.014，+Z 从 8.692 变为 7.266。
- 六次轴向拖动均只改变对应输入，其余五项范围、模型初始矩阵和相机姿态不变。六条可见半轴悬停均能显示对应标签。
- 上述覆盖联合六面／六向，不代表独立模式所有方向、旋转后六面、极小长方体或 Gaussian 画面完整通过。截图准备证据保存在本地忽略目录 `runtime/box-faces-fit-canvas.png`、`runtime/axis-six-directions-ready.png`。

## 2026-09-10：联合轴向范围与手柄

使用真实 PLY／PCD 保留会话 `a102d3c7-eaef-4642-9624-c13464deee8c`，生产构建和 1920×1080 视口。未重新运行 ICP，也未保存业务矩阵。

- 打开联合坐标轴剖切，悬停正 X 半轴显示 +X 箭头。
- 拖动 +X 箭头，最大范围由 `12.689` 回写为 `9.975`，最大边界自动启用；模型粗配准初始矩阵文本保持不变。
- 重新加载后将表单 X 最大范围改为 `5`，+X 标签横向位置由 `1223.78px` 更新为 `1111.2px`，确认表单状态传回手柄。
- 输入 X 最小值 `4`，随后将最大值输入为 `2`，界面限制最大值为 `4.000`。
- 重置轴向范围后，最大边界复选框恢复关闭。

轴向和长方体分别保留同名标签，自动化应区分标签所属类型或可见状态。点击位置依赖窗口和相机，测试时应按当前截图定位，不能将未命中视作有效拖动。

## 待验证

- 联合其余五个方向及 A／B 独立剖切手柄。
- 拖动达到对侧边界时的画面与手柄表现。
- 中心点及 Gaussian 剖切画面的完整专项验收。

## 2026-09-10：独立手柄与边界限制

- A／B 均启用轴向剖切，选择编辑 B，拖动 +X 后只有 `ind-b-x-max` 由 `12.689` 变为 `10.516`，B 最大边界启用；A 与联合范围均未变化，模型初始矩阵不变。
- 切换编辑 A，同方向拖动后只有 `ind-a-x-max` 变为 `9.429`，A 最大边界启用；B 与联合范围均未变化，模型初始矩阵不变。
- 将 A 最小 X 设为 `9`，继续向内拖动最大 X 手柄，最终最小／最大均为 `9.000`，未越过对侧边界。
- 再次选择编辑 B，其最大 X 仍为 `10.516`；关闭面板后剖切激活态保持。
- 本轮覆盖 A／B 独立 +X 路由和一次拖动限界，未覆盖其余五个方向或 Gaussian 画面，未修改运行逻辑。

## 2026-09-11：完整 Gaussian 轴向剖切与切回

- 会话 `a102d3c7-eaef-4642-9624-c13464deee8c`，A 为 3,777,901 点、256,898,154 字节 Gaussian PLY；1440×900 浏览器真实加载成功，按钮变为「A：点云」。
- 隐藏 B，启用联合坐标轴剖切，X 最大范围设为 −2；截图确认 Gaussian 出现切面。切回中心点后相同边界保留，结果矩阵及指标文本未改变。
- 仅在切回操作期间记录 WebGL2 删除调用：deleteTexture 8 次、deleteBuffer 2 次。说明该操作实际触发 GPU 资源删除，不代表完整显存计量或长期无泄漏。
- 截图保存在本地忽略目录：`runtime/gaussian-full-before-clip.png`、`runtime/gaussian-full-clipped.png`、`runtime/gaussian-back-to-points.png`。
- 另以拦截 Gaussian 请求模拟无效响应，验证加载时按钮锁定、错误后恢复中心点并允许重试。模拟错误会产生预期控制台错误，不计为真实成功加载错误。
